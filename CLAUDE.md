# Expressive RGB

Android Bluetooth LE RGB LED controller app targeting DuoCo hardware.

## Development workflow

This project is developed across three tools with different roles:
- **Claude (chat)** — architecture decisions, planning, prioritization. This is where design tradeoffs get decided.
- **Gemini AI Studio** — some code generation, prompted with complete self-contained instructions and explicit file-scope constraints.
- **Claude Code (this environment)** — direct-repo execution and verification. Your job is to check claims against the real source, not trust prior self-reports (from Gemini or from Claude chat) at face value.

**Verification standard: treat any single AI's self-report as unverified until checked against real source, a build, or git history.** Full-file dumps from an AI chat can be fabricated convincingly even when they look detailed and specific. The trustworthy channels are: the actual files in this repo, builds that are run and pass/fail for real, and git history inspected directly. If you're asked to confirm something works, verify it against the actual code — don't just repeat back what you were told.

Joe (the developer) considers himself a git novice — explain git concepts (staged vs. committed, what a diff/branch represents) rather than just running commands silently when it's relevant to what he asked.

## Architecture

Target architecture is **MVI / unidirectional data flow**, chosen over MVVM+UseCases because concurrent async streams (BLE, audio, UI events) were causing race conditions under plain MVVM.

Target package structure:
- `core/` — pure logic (color math, calibration, protocol encoding)
- `data/` — DB, prefs
- `domain/` — models, repository interfaces
- `hardware/` — BLE, audio, camera
- `presentation/` — Compose UI, ViewModels

Two "god files" are mid-refactor into this structure:
- `RgbControllerViewModel` — originally 5,440 lines, 12 mixed responsibilities (BLE GATT, DSP/beat detection, audio capture, DB, prefs, scene orchestration)
- `MainActivity` — originally 2,476 lines (nav/permissions/dialogs/UI)

### Refactor phases
1. **Phase 1 — COMPLETE.** Extracted pure/stateless logic into `core/`: `ColorConverter`, `CalibrationMatrixSolver`, `DuoCoProtocol`, `ProceduralSceneParams`.
2. **Phase 2 — COMPLETE.** Removed Hilt DI entirely (manual DI: `AppContainer`, `ViewModelFactory`). Fixed `AppPreferencesRepository` domain/UI layering violation.
3. **Phase 3a — COMPLETE.** `DuoCoProtocol.overrides` encapsulated (private + accessors). Connection state centralized via `ConnectionManager` singleton (kills the old static-field hazard where `manuallyDisconnected` was written by the ViewModel and read directly by `MainActivity`, bypassing `StateFlow`).
4. **Phase 3b — COMPLETE.** State migrated to `RgbUiState` + intent reducer pattern, all four reducers written/tested/merged, and — as of the `wire-phase3b-reducers` branch — all four are wired live into `RgbControllerViewModel` via `dispatch(RgbIntent)`. See "RgbUiState shape" and "Current reducer status" below.
5. **Phase 4 — audio capture hardware isolation COMPLETE.** Branch `phase4-hardware-isolation`, based on `wire-phase3b-reducers`; PR #12 merged into `wire-phase3b-reducers` 2026-07-20. `wire-phase3b-reducers` (including Phase 4) was then merged into `refactor-checkpoint` via PR #13 (2026-07-20, merge commit `d2024cc`) — `refactor-checkpoint` now contains all of Phases 1-4. **Promoted to `main`** via PR #14 (2026-07-20) once the on-device smoke test of music sync (`phone_mic` and `on_device` modes) was confirmed done — see "Git/GitHub notes" below. BLE write/connection I/O and the MediaProjection screen-capture path used by Ambiance mode were judged out of scope for this slice and deliberately not touched — see below.
6. **Post-Phase-4 cleanup COMPLETE.** PR #15 (merged into `refactor-checkpoint` 2026-07-20, then promoted to `main` via PR #16) closed out a post-refactor audit: removed confirmed-dead code (`RgbControllerViewModel.delayMs`, `MainActivity.validateHex`, unused `MediaProjectionManager` imports, a dead unread `pacingPrefs` handle in `AmbianceScreen.kt`), added previously-missing test coverage for `ConnectionManager`, `CalibrationMatrixSolver`, `DuoCoProtocol` (14 of ~20 public functions had no tests — the highest-risk untested code in the app, since it encodes real BLE hardware commands), `AudioDspProcessor`, and `Fft.kt`, and consolidated four duplicated raw `"ble_pacing_prefs"` SharedPreferences call sites (`SettingsTabContent.kt`, `AmbianceProcessor.kt`, `AmbianceOutputInterpolator.kt`) behind `AppPreferencesRepositoryImpl.getPacingPrefInt`. Full suite went from 150 → 205 tests. See "Testing" below for the per-file breakdown.

   **What "`startAudioEngine`/`audioThread`" actually was.** There was never a separate `audioThread` function — it was a `Thread{}` lambda assigned to a field, both halves of one 869-line function, `startAudioEngine(mode: String)`, mixing two hardware capture backends (Visualizer API ~20Hz callback-driven for `"on_device"`, raw `AudioRecord` ~43Hz thread-driven for `"phone_mic"`/other modes) with near-duplicate inline DSP and direct `_uiState`/`_telemetry` writes that bypassed the reducer (a deliberate, documented Phase 3b exception, not an oversight).

   **What changed.** New package `hardware/audio/`:
   - `AudioCaptureSource.kt` — interface (`start(onFrame, onLog, isRunning, onError): Boolean`, `stop()`) + `AudioBackend` enum + `AudioCaptureFrame` data class (carries `magnitude`, `realBins`/`imagBins`, `numBins`, `backend`, `timestampMs` — real/imag bins included because `BeatDetector`'s phase-flux math needs them, not just magnitudes).
   - `AudioRecordCaptureSource.kt` — real `"phone_mic"` backend (AudioRecord + manual Hamming-windowed FFT thread, same thread name/priority/buffer-size logic as before).
   - `VisualizerCaptureSource.kt` — real `"on_device"` backend (Android `Visualizer` API session).
   - `Fft.kt` — moved verbatim from the ViewModel (project FFT utility, not an Android API).
   - `FakeAudioCaptureSource.kt` (test tree, `hardware/audio/`) — synchronous, deterministic: `emitFrame()`/`emitError()` invoke the stored callbacks directly, no real threading, tracks `stopCallCount`/`isStarted` for assertions.

   New package `core/audio/`:
   - `AudioDspProcessor.kt` — the DSP pipeline (noise gate, 3-band energy, EMA smoothing, beat detection via the now-`internal` `BeatDetector`, palette cycling, hue/gamma/saturation/value, idle detection, 8-band UI amplitudes) extracted as a pure, hardware-free class. One instance per engine run (matches the original reset-on-start behavior of its formerly-function-local state). `process(frame, settings, nowMs): AudioDspResult?` — nullable to mirror the Visualizer backend's original frame-skip when `numBins < 349`. **Deliberately not unified**: AudioRecord-backend and Visualizer-backend bin-range constants stay backend-specific inside `process()`, per the sample-rate-mismatch deferred issue below — do not "fix" this as a side effect of touching this file.

   `RgbControllerViewModel.startAudioEngine` is now a ~50-line orchestrator: construct the right `AudioCaptureSource` + a fresh `AudioDspProcessor`, wire `onFrame` to `processor.process(...)?.let { publishAudioDspResult(it) }`. `stopMusicSyncInternal` now calls `audioCaptureSource?.stop()` instead of touching `AudioRecord`/`Visualizer` fields directly. `BeatDetector` widened from `private` to `internal` (stayed in `RgbControllerViewModel.kt`, not moved) so `AudioDspProcessor` can reuse it. Net effect: `RgbControllerViewModel.kt` went from 4,778 → 3,925 lines.

   **Preserved as-is, not fixed (by design):** the sample-count-vs-time-based DSP constant mismatch between the two backends (see "Deferred / known-not-urgent" below); the latent shutdown-latency issue where `AudioCaptureSource.stop()` on the AudioRecord backend can't reliably interrupt a blocking `AudioRecord.read()` call (actual thread exit happens on the next loop check after `read()` returns); `runAudioSimulationEngine()` (untouched fallback), `DeviceWriteManager`, and the BLE write path downstream of `queueCommand` (untouched, out of scope).

   **Explicitly out of scope for this slice, not overlooked:** BLE write/connection I/O (the existing `DeviceWriteManager`/`ConnectionManager` machinery was judged sufficient/separate enough not to need re-isolation here) and the `MediaProjection` screen-capture path for Ambiance mode (no camera hardware is in use — see "Ambiance mode" below — and its capture lifecycle is driven from `MainActivity` via `ActivityResultLauncher`, not from the ViewModel, so it didn't share a natural interface boundary with the audio work).

   **Real hardware behavior caveat, same as Phase 3b, now resolved:** no real on-device mic/Visualizer hardware behavior is exercised by unit tests (Robolectric shadows don't model real audio input) — only construction/contract (`AudioCaptureSourceTest.kt`) and the pure DSP math (`AudioDspProcessorTest.kt`/`AudioDspProcessor` expansion in PR #15) are unit-tested. The manual on-device smoke test of music sync (both `phone_mic` and `on_device` modes) that this gated on has since been run and confirmed — see "Git/GitHub notes".

7. **Phase 5, part A — MainActivity tab extraction + PacingAutoTuneDialog algorithm extraction COMPLETE.** Two independent, non-overlapping extractions done in parallel on `phase5-mainactivity-tabs` and `phase5-pacing-autotune-extraction`, combined on `phase5-tab-and-pacing-extraction`, merged into `refactor-checkpoint` via PR #17 (2026-07-20), then promoted to `main` via PR #18 (2026-07-20) — see "Git/GitHub notes".
   - `MainActivity.kt`: 2,482 → 1,131 lines. Extracted the two remaining un-extracted tabs (Home `selectedTab==0`, Devices `selectedTab==2`) into `HomeScreen.kt` / `DevicesScreen.kt` under `ui/components/`, mirroring the existing `ModesScreen`/`MusicScreen`/`AmbianceScreen` pattern.
   - **Incidental behavior fix, bundled deliberately, not silent:** the BLE "connecting…" overlay (`AnimatedVisibility`/`isConnecting`) used to be nested inside the tab-conditional `else{}` block (a brace-scoping artifact), so it only ever rendered on tabs 0/2/3. It's now a sibling of the whole tab-dispatch `if/else if` chain inside the outer `Box`, so it renders consistently across all six tabs. **Verified directly against source during merge review** (not just the PR description) — confirmed the overlay block moved from inside the old `else` branch to after the `if/else if` chain closes, with no other logic changes riding along.
   - `PacingAutoTuneDialog.kt`: 517 → 429 lines, now UI-only. The embedded burst/fine-tune/stress-test auto-tune state machine moved to `core/pacing/PacingAutoTuneEngine.kt` — pure, hardware-free, same pattern as `AudioDspProcessor` (Phase 4). 23 new tests in `PacingAutoTuneEngineTest.kt`.
   - On-device smoke tests (Home/Devices tab rendering, overlay appearance on Modes/Music/Ambiance tabs, pacing auto-tune sequence correctness) were listed as follow-up in the PR, not yet confirmed done as of this writing — check before relying on them being verified.

8. **Phase 5, part B — getActiveInstance() escape-hatch closure + BeatDetector/MetronomePlayer/demo-DSP extraction COMPLETE.** Two sequential, non-overlapping slices in the same file (`RgbControllerViewModel.kt`), each its own branch/PR against `refactor-checkpoint`, then both promoted to `main` together via PR #21 (2026-07-20) — see "Git/GitHub notes".
   - **Step 1 (PR #19, branch `phase5-close-getactiveinstance-escape-hatch`):** `AmbianceCaptureService`/`AmbianceOutputInterpolator` were calling `RgbControllerViewModel.broadcastCommand()`/`writeAmbianceColor()` directly through the static `getActiveInstance()` singleton, bypassing `dispatch()` entirely — reopening the exact anti-pattern the MVI refactor eliminated elsewhere. Fixed by wiring the previously-defined-but-unhandled `WriteAmbianceColor` intent into `AmbianceSettingsReducer` (new `AmbianceSideEffect.WriteColor`) and adding a new `SetAmbianceCaptureActive` intent for the capture-session phone-mic toggle (new `AmbianceSideEffect.BroadcastCommand`). Both effects mirror the exact prior hardware calls (the `AmbianceCaptureState.isActive` guard + `broadcastCommandDirect` for the color write; `broadcastCommand` for the toggle) — a wiring fix, not a behavior change. `writeAmbianceColor()` is now a one-line `dispatch(...)` call; a new `setAmbianceCaptureActive()` wrapper replaces the direct `broadcastCommand()` calls in `AmbianceCaptureService`. `getActiveInstance()` itself is **not** removed — it remains the necessary bridge for obtaining the VM reference from non-Compose classes (the `Service` and the `HandlerThread`-driven interpolator have no DI path to the ViewModel); only the bypassed-dispatch calls made *through* it were the target. This also **supersedes** two bullets in the "Current reducer status" `AmbianceSettingsReducer` write-up below (kept there for history, corrected here): `WriteAmbianceColor` is no longer "belongs with Commands/Protocol, not settings" — it's now handled by this reducer — and ambiance capture start/stop now *does* have a ViewModel intent (`SetAmbianceCaptureActive`) alongside the still-true fact that `MainActivity`/`AmbianceCaptureService` drive the actual `MediaProjection` capture lifecycle directly.
   - **Step 2 (PR #20, branch `phase5-beatdetector-metronome-demodsp-extraction`):** same idea as Phase 4's audio-hardware isolation — pulled three more things out of the ViewModel that aren't tangled up with its other responsibilities. `BeatDetector` (~500 lines, autocorrelation/phase-flux beat detection) moved verbatim to `core/audio/BeatDetector.kt` — pure computational logic, so it belongs alongside `AudioDspProcessor` rather than at file scope in the ViewModel (this **supersedes** Phase 4's note above that it "stayed in `RgbControllerViewModel.kt`, not moved" — visibility stays `internal`, which is module-visible in Kotlin, so no import changes were needed at any of its construction call sites beyond `AudioDspProcessor`'s now-redundant same-file import). `MetronomePlayer` (raw `AudioTrack` click synthesis) moved verbatim to `hardware/audio/MetronomePlayer.kt` — it drives real audio-output hardware, unlike the pure-logic `core/audio/` classes. The ~300-line hand-duplicated demo DSP pipeline inside `runAudioSimulationEngine()` (the `java.util.Random`-driven fallback used when real `AudioRecord`/Visualizer capture fails to start — **this also supersedes** Phase 4's note that this function was "untouched") was extracted to `core/audio/DemoAudioDspSimulator.kt`, matching the `AudioDspProcessor` split: a pure `process(state): AudioDspResult` method (reusing the same `AudioDspResult` type the real pipeline produces) holding the per-run mutable DSP state. `runAudioSimulationEngine()` is now a ~25-line orchestrator that ticks a fresh simulator on the same 23ms cadence and publishes through the existing `publishAudioDspResult()` helper — the same one the real capture pipeline uses, so the demo path no longer hand-writes its own `_uiState`/`_telemetry`/`queueCommand` block. The now-unused private `hsvToRgb()` ViewModel wrapper was removed as dead code. **Deliberately NOT unified:** `DemoAudioDspSimulator` still shares no code with `AudioDspProcessor`/`BeatDetector` — it's randomness-driven, not real beat detection on FFT bins; preserved as documented behavior, not reconciled. `RgbControllerViewModel.kt`: 3,925 → 3,105 lines.
   - Manual on-device smoke test of the ambiance capture path (step 1) and the calibration metronome click / demo-visualizer fallback (step 2) not yet confirmed done as of this writing — check before relying on them being verified.

9. **Phase 6 — BLE hardware extraction, calibration matrix extraction, scene orchestration extraction, and (for real this time) closing the `getActiveInstance()` escape hatch. COMPLETE, promoted to `main`/`refactor-checkpoint` via PR #22 (2026-07-21) — see "Git/GitHub notes".** Four independent slices, each its own branch/PR against `phase6-godfile-extraction` (based on `main`), merged together with manual conflict resolution (all four touched `RgbControllerViewModel.kt`'s constructor/DI wiring). `RgbControllerViewModel.kt`: 3,103 → 2,335 lines. Full suite: 235 → 253 tests.

   **Correction to Phase 5, part B's claim.** That entry says PR #19 "closed" the `getActiveInstance()` escape hatch and that `getActiveInstance()` itself was deliberately *not* removed. Both were true only for the *ambiance dispatch-bypass* half of the problem. Verified against real source at the start of this phase (not taken on CLAUDE.md's word): `getActiveInstance()` was still called directly at 3 external sites (`AmbianceOutputInterpolator.kt:112`, `AmbianceCaptureService.kt:75,162`) to *obtain* the VM reference (even though what happened after obtaining it was already clean `dispatch()` calls), plus 5 internal self-references inside `RgbControllerViewModel`'s own `gattCallback` (`getActiveInstance() ?: this@RgbControllerViewModel`, an unrelated BLE-callback-routing hazard, not an ambiance issue). This phase closed both halves, and a repo-wide grep after both landed confirmed zero remaining callers — so the companion-object `instance` field and `getActiveInstance()` getter were removed entirely (done at the final merge step, not on either sub-branch, since removal was only safe once *both* halves were fixed).

   **BLE hardware layer extraction** (branch `phase6-ble-hardware-extraction`, highest-risk slice, done on Opus given the stakes). New package `hardware/ble/`: `BleScanTransport`/`AndroidBleScanTransport` (wraps `BluetoothLeScanner`/`ScanCallback`), `BleGattTransport`/`AndroidBleGattTransport` (wraps `BluetoothGatt`/`BluetoothGattCallback`), and `DeviceWriteManager` (moved from a ViewModel inner class to a top-level class in the same package, decoupled from `prefsRepo`/`_telemetry`/calibration via constructor callbacks instead of direct field access). Same split as Phase 4's audio isolation: raw Android API calls moved, but *decision* logic stayed in the ViewModel — `handleConnectionStateChange`'s exponential-backoff retry, `handleScanResult`'s auto-connect/dedup, and `queueCommand`/`broadcastCommand`/`sendCommandToDeviceDirect`'s addressing/exclusion-set logic are all still ViewModel-side, now driving the transport through callback lambdas instead of the ViewModel implementing `BluetoothGattCallback` directly. The companion-object statics `activeConnections`/`writeCharacteristics`/`retryAttempts`/`deviceWriteManagers`/`connectionScope` moved to instance fields on `AndroidBleGattTransport`, which is constructed once via `AppContainer` (`by lazy`, same pattern as `ConnectionManager`) so BLE state still survives ViewModel recreation without needing a ViewModel-side static singleton. `BleGattTransport.registerCallbacks()`/`registerWriteHooks()`, called once from the ViewModel's `init {}`, re-point the transport's live callback references at the current VM instance — this is what actually removed the `gattCallback` self-reference hunt (the transport always calls whichever callback set was most recently registered, so there's no more need to go looking for "the active instance"). Dead code removed as a confirmed side effect: `syncPhysicalBulbOfDevice(gatt)` (defined, zero call sites, referenced the now-moved `deviceWriteManagers` map) and the no-op `handleCharacteristicRead`/`handleCharacteristicChanged` overrides. New fakes `FakeBleScanTransport`/`FakeBleGattTransport` (`hardware/ble/` test tree) follow `FakeAudioCaptureSource`'s exact synchronous-callback-store pattern. `RgbControllerViewModel.kt`: 3,103 → 2,836 lines on this slice alone.

   **Calibration matrix math extraction** (branch `phase6-calibration-matrix-extraction`, independent, low-risk — all four target functions were already pure). `getCalibrationMatrices`, `interpolateMatrices`, `applyCalibrationIfRequired` moved verbatim to new `core/calibration/CalibrationMatrixOps.kt`, alongside the existing `CalibrationMatrixSolver`. `applyCalibrationIfRequired` is preserved exactly as its pre-existing disabled stub (comment: "Old full-spectrum RGB calibration is entirely disabled per scope change") — **not** re-enabled, just relocated. The ViewModel's `fit3x3Matrix` wrapper (already just a one-line delegate to `CalibrationMatrixSolver.fit3x3Matrix`) was removed entirely as confirmed-dead code — a repo-wide grep found zero callers of the wrapper (both `CoreExtractionTest.kt` and `CalibrationMatrixSolverTest.kt` already called `CalibrationMatrixSolver.fit3x3Matrix` directly). `processCommandWithCalibration` stays in the ViewModel as the thin call site, now calling `CalibrationMatrixOps.applyCalibrationIfRequired(...)`, and is what `AndroidBleGattTransport`'s `DeviceWriteManager` calls through its `calibrate` constructor callback. New `CalibrationMatrixOpsTest.kt` (11 tests) covers `getCalibrationMatrices`/`interpolateMatrices`, previously untested.

   **Scene orchestration extraction** (branch `phase6-scene-orchestration`, based on `phase6-ble-hardware-extraction` since `applyDeviceState` calls `sendCommandToDeviceDirect` directly — needed BLE's finished public surface first). New `domain/SceneManager.kt`: `captureCurrentDeviceState`, `saveScene`, `updateScene`, `deleteScene`, `renameScene`, `saveAiSceneSequence`, `updateAiSceneSequence`, `applyScene`, `applyDeviceState`, `updateDeviceStateInMap` moved verbatim, plus ownership of `sceneChainJob`/`isApplyingScene`/`cancelSceneChain()` (only touched by scene code + a few effect executors). **DI shape deliberately differs from `ConnectionManager`'s precedent**: `SceneManager` is constructed *inside* the ViewModel via `by lazy`, not via `AppContainer`, because — unlike `ConnectionManager`, which owns its own state — `SceneManager` *borrows* live ViewModel-internal mutable state (`_uiState`, `_scenes`, `customModes`, `sceneRunners`, `activeExcludedMacs`, `deviceStateStore`) that doesn't exist until the ViewModel itself is constructed. It takes `viewModelScope` as its coroutine scope, so the scene-chain job and save-state coroutines still auto-cancel on `onCleared()` with no extra plumbing. The ~30 command/setter calls it needs (`sendCommandToDeviceDirect`, `setPower`, `setColor`, `applyAmbiancePreset`, etc.) are bundled into a `SceneCommandSink` built from method references, rather than 30 individual constructor lambdas — flagged as a faithful measure of how coupled scene application already is to the ViewModel's public setter surface, not something reducible without a behavior change; a candidate for a later slice to route through `dispatch(RgbIntent...)` instead, out of scope here. **Preserved as-is, not fixed:** `applyScene`'s prefs-persistence side effects executed from inside a `_uiState.update {}` lambda; the recursive scene-chain self-launch (`viewModelScope.launch { delay(...); applyScene(nextScene, ...) }`); the direct cross-layer reach-ins to `AmbianceCaptureState`/`AmbianceCaptureService` (Toast + `Handler.post`) and `ui.components.ColorUtils` from `applyDeviceState`/`updateDeviceStateInMap` — known layering warts, not this phase's job. `RgbControllerViewModel.kt`: 2,836 → 2,418 lines on this slice.

   **Ambiance sink DI** (branch `phase6-ambiance-sink-di`, independent, low-risk). New `domain/AmbianceCommandSink.kt` — a plain class (no interface abstraction beyond its own single-listener `Listener` sub-interface, no DI framework) holding one `@Volatile var listener: Listener?` slot, constructed once via `AppContainer` (`by lazy`, same `ConnectionManager` precedent). `RgbControllerViewModel` implements `AmbianceCommandSink.Listener` (the existing `writeAmbianceColor()`/`setAmbianceCaptureActive()` one-line `dispatch()` wrappers gained `override`, no behavior change), registers itself in `init {}`, and clears the slot in `onCleared()` (guarded by identity check against a possible newer instance). `AmbianceCaptureService` (an Android `Service`, no DI framework reach) and `AmbianceOutputInterpolator` (plain class, only ever constructed by the Service) now reach the sink via `(application as RgbControllerApplication).container.ambianceCommandSink` — `RgbControllerApplication` already exposed `container: AppContainer` publicly, so no new plumbing was needed there.

   **Merge conflicts, resolved by hand at the final integration step** (all four branches modified `RgbControllerViewModel.kt`'s constructor/`init`/`onCleared`/companion object independently from the same base): `RgbControllerViewModelFactory`, `AppContainer`, `MainActivity`, and the ViewModel constructor all needed both the 2 new BLE-transport params *and* the 1 new `ambianceCommandSink` param combined (order: `bleScanTransport, bleGattTransport, ambianceCommandSink`, consistently across all four call sites); the `init {}` block needed both the BLE `registerCallbacks`/`registerWriteHooks` calls *and* `ambianceCommandSink.listener = this`; the companion object's `instance`/`getActiveInstance()` removal (see correction above) was the one change made at merge time rather than on either sub-branch, since it was only safe once both halves' external/internal callers were gone.

   **On-device smoke test** (same standing requirement as every prior phase — Robolectric/JVM tests cannot exercise any of this): BLE scan/connect/disconnect/reconnect against real DuoCo hardware; ambiance capture start/stop (verifies the new `AmbianceCommandSink` wiring actually reaches the ViewModel, not just that it compiles); applying a saved scene, including a chained scene (verifies `SceneManager`'s extraction didn't break command dispatch or the recursive chain self-launch); the calibration flow (verifies the relocated matrix math still calibrates real hardware output correctly). Confirmed done by the developer, gating promotion via PR #22 — see "Git/GitHub notes".

10. **Color-conversion dedup — COMPLETE, merged to `main` via PR #23 (2026-07-21, merge commit `1148672`), skipping the on-device smoke test by Joe's explicit choice.** Not a god-file phase (doesn't touch `RgbControllerViewModel`/`MainActivity`); a standalone cleanup of duplicated color math flagged separately. Branch `dedup-color-conversion`, off `main`, left undeleted per standing convention.
    - `core/color/ColorConverter.kt` gained three new functions: `srgbToLinear(Int): Double`, `linearToSrgb(Double): Int` (moved verbatim from private copies in `ambiance/AmbianceProcessor.kt` and `ambiance/AmbianceOutputInterpolator.kt` — the two copies were byte-identical, confirmed by direct comparison before touching either file), and a new `luminance(r: Double, g: Double, b: Double): Double` (Rec. 709 coefficients, scale-agnostic) replacing four inline repeats of the same `0.2126/0.7152/0.0722` expression in `AmbianceProcessor.kt` (the pixel-scanning `getLum()` helper plus three luminance calculations in the EMA/scene-cut logic).
    - **Deliberately NOT unified: `AmbianceProcessor`'s private `hsvToRgb`/`rgbToHsv` were left as-is, not merged into `ColorConverter.hsvToRgb`.** Diagnostic pass before changing anything found these are not actually byte-identical duplicates despite looking like one at a glance: `ColorConverter.hsvToRgb` applies an extra cubic perceptual-brightness correction step (`(channel/255)^3 * 255`) that the ambiance version has never had, plus a rounding difference (`toInt()` truncation in `ColorConverter` vs `roundToInt()` in `AmbianceProcessor`). Forcing them together would visibly darken every ambiance frame — a real behavior change to a live visual pipeline, not a safe refactor. Left in place with an explanatory comment at the call site instead of guessing which behavior was "correct."
    - `rgbToHsv` in `AmbianceProcessor.kt` has no counterpart in `ColorConverter` at all (nothing to dedupe against) — untouched.
    - No new tests: pure verbatim relocation of the two gamma functions plus a mechanical extraction of an already-inline expression into a named function, same rationale as the Phase 6 `SceneManager`/`AmbianceCommandSink` moves. Full suite unchanged at 253 tests, all green.
    - **On-device smoke test was explicitly skipped for this merge, by Joe's choice — not an oversight.** This breaks the standing pattern every prior phase followed (gate merge on a confirmed on-device check of the touched visual/hardware pipeline). Flagged plainly at merge time. If the ambiance screen-capture color output looks visually different after this, the unverified diagnostic pass above (byte-identical gamma functions, deliberately-preserved `hsvToRgb` divergence) is the first thing to re-check against real device output.

11. **Gradle wrapper bump to 9.5.0 — committed directly to `main` (2026-07-21), separate from the color-conversion merge.** Re-applied via Android Studio's AGP Upgrade Assistant after an earlier accidental git checkout wiped the original version bump (see the AI-Studio-GitHub-panel caveat below — unrelated cause, just noting the wrapper had been in a modified-but-uncommitted state for a while). Only `gradle/wrapper/gradle-wrapper.properties` changed (`distributionUrl`/`distributionSha256Sum`, gradle-9.3.1-bin → gradle-9.5.0-bin); `gradle.properties` and `gradle/libs.versions.toml` were checked and are not part of this bump. `./gradlew assembleDebug` confirmed green after commit.

12. **BLE GATT lifecycle extraction — investigated, found already done, no action taken (2026-07-21).** A request came in to extract `handleConnectionStateChange`/`handleServicesDiscovered`/`handleCharacteristicChanged`-style GATT callback handling from `RgbControllerViewModel.kt` into `hardware/ble/`, on the assumption this was leftover from Phase 6. **Verified against real source before doing anything — the assumption was wrong.** Phase 6's BLE hardware extraction already fully moved the actual `BluetoothGattCallback` implementation into `AndroidBleGattTransport` (`hardware/ble/BleGattTransport.kt`), which invokes three thin ViewModel handlers via `registerCallbacks()` (wired once in `RgbControllerViewModel.init`, line ~785) — there's an explicit comment at `RgbControllerViewModel.kt:1627-1631` documenting this. There is no lingering `BluetoothGattCallback` implementation, no MTU/RSSI/descriptor/characteristic-changed handling anywhere in the ViewModel (the app never had any — DuoCo's protocol is write-only), and zero external callers of these handlers outside the ViewModel. What remains in `handleConnectionStateChange` (exponential backoff retry: attempt cap, `2^n` delay math, re-eligibility checks) and `handleScanResult`/`addSimulatedDevice` (duplicated auto-connect eligibility checks) is decision logic that, per this file's own Phase 6 convention (decision logic in the ViewModel, hardware I/O in `hardware/ble/`), correctly belongs where it is. Moving it into `hardware/ble/` would have cut against that precedent for no benefit. **No branch was opened; nothing merged.** A smaller, real cleanup opportunity was identified but not acted on (Joe's call whether to pursue it later): `handleCharacteristicWrite` (`RgbControllerViewModel.kt:1702-1708`) is a pure passthrough with no decision logic (just a log line + `bleGattTransport.notifyWriteCompleted(address)`) and could be inlined as a lambda rather than a named function; the auto-connect eligibility check is duplicated near-verbatim between `handleScanResult` (~1424-1434) and `addSimulatedDevice` (~1504-1514) and could be consolidated into one helper.

13. **Audio pipeline diagnostic instrumentation — added and since analyzed (2026-07-21).** Temporary `DiagnosticLogger.log()` calls added to gather real on-device data on why beat detection/color response feels off. The resulting data fed the item 16/17 tuning pass and the item 24 noise-floor/tempo-lock fixes below — kept here as reference methodology for any future capture, since the same steps and log tags are still live in the app.
    - **Backend sample-rate/bin values found in source** (both previously undocumented in this file): `VisualizerCaptureSource.kt`'s `onFftDataCapture` receives a `samplingRate` param (Android Visualizer API convention: **milliHz**, so e.g. `44100` means 44.1Hz — needs converting/confirming against the actual logged value, don't assume) that was declared but never read; `captureSize` is `Visualizer.getCaptureSizeRange()[1]` (device-dependent max, not a fixed constant) and `numBins` is `fft.size / 2` (guarded to skip anything under 349 bins, matching `AudioDspProcessor`'s existing `numBins < 349` check). `AudioRecordCaptureSource.kt` is fixed at 44100Hz / 1024-sample buffer / 512 bins, as CLAUDE.md already stated. A one-time `"BackendInfo"` log line was added to each (`VisualizerCaptureSource.kt`'s `onFftDataCapture`, gated by a new `loggedBackendInfo` flag so it only fires once per engine run; `AudioRecordCaptureSource.kt`, right after `record.startRecording()`) logging `backend`/`samplingRate`/`captureSize`/`numBins` so the two can be compared directly in one exported file — this is the direct instrumentation for the sample-rate/bin-mismatch hypothesis already documented under "Deferred / known-not-urgent".
    - **Beat confidence logging** added in `AudioDspProcessor.kt`'s `process()`, right after the existing `beatDetector.process(...)` call (~line 214): logs `backend`/`isBeat`/`strength`/`confidence`/`bpm`/`bpmConfidence` under tag `"BeatConfidence"` every time `isBeat == true`, plus a throttled (every 1500ms) sample when `isBeat == false` for baseline context — same `if` condition handles both cases to avoid a duplicate branch, using a new `lastDiagnosticSampleMs` field (mirrors the existing rolling-history instance-field pattern in this class).
    - **Frame timing logs** (`"FFT capture interval"` in `VisualizerCaptureSource.kt`, `"MIC read callback interval"` in `AudioRecordCaptureSource.kt`) were checked, not added — both already existed and are already gated through `DiagnosticLogger.log()` (which itself no-ops unless `DiagnosticLogger.isRecording()`), so no change was needed there; confirmed they'll show up in an export once recording is enabled.
    - **On-device steps to actually gather data** (read directly from `SettingsTabContent.kt` and `MusicScreen.kt` — do not guess at labels if these files change):
      1. Home tab bar → **Settings** tab → scroll to the **Experimental** card → tap it to expand.
      2. Inside Experimental, scroll to **"LED Freeze Diagnostic Recorder"** → tap **"Start Diagnostic Recording"** (this button also does double duty for this audio diagnostic pass — it's the same in-memory ring buffer `DiagnosticLogger` that both LED-freeze and audio logging now write to).
      3. Go to the **Music** tab → tap the **"Phone Mic"** card (grants RECORD_AUDIO permission if not already granted) → play the chosen song for **60–90 seconds**.
      4. Tap the **"Phone Mic"** card again to stop sync (it toggles to stop when active).
      5. Tap the **"On Device"** card → play the **same song** again from the start, for another 60–90 seconds.
      6. Tap **"On Device"** again to stop sync.
      7. Back in Settings → Experimental, tap **"Stop Diagnostic Recording"** (same button, now showing the stop label/icon) — this automatically exports the log to a file and shows a "Recording stopped. Log exported!" toast.
      8. A new **"Export Log & Share"** button appears below it once stopped — tap it to open the Android share sheet and send the log file back (e.g. to Joe's own email/Drive/whatever chooser target is easiest to then hand off).
    - **Analysis (superseded this line's original "not yet analyzed" framing):** the `BeatConfidence`/`BackendInfo` data from captures made with these steps is what item 16's confidence-gating/strength-curve tuning and item 17's near-silence false-beat fix were built from, and later captures (via the adb harness, item 25) drove the tempo-lock and noise-floor fixes in item 24. The sample-rate/bin-mismatch hypothesis remains real but deliberately deprioritized (see "Deferred / known-not-urgent" below).

14. **`StartAudioEngine` main-thread-freeze fix — applied, NOT confirmed against a real crash/ANR trace (2026-07-21).** Joe hit a real freeze/crash on-device (phone mic on → play ~60s → mic off → on-device on → freeze then crash) but could not reproduce it a second time, and the one Logcat capture taken was stale (old entries from a different app — no real trace of this bug was ever captured). **This fix is a source-level correction of a genuine, confirmed-by-reading-the-code defect (a missing dispatcher wrap, inconsistent with every sibling effect handler), applied because it plausibly explains the symptom Joe saw once — not a root cause confirmed by an ANR/crash trace.** If the freeze/crash recurs after this, treat this fix as ruled out and look elsewhere; don't assume it's "already fixed."
    - **What was confirmed by reading current source (both before and after the fix, not assumed from the hypothesis alone):** in `RgbControllerViewModel.kt`'s `dispatch()` → `executeAudioSideEffect()`, every effect case that does real work wraps itself in `viewModelScope.launch(Dispatchers.IO) { ... }` except `AudioSideEffect.StartAudioEngine`, which called `startAudioRecording(effect.mode)` synchronously on whatever thread `dispatch()` ran on (main, for a UI tap). `startAudioEngine`'s on-device branch calls `VisualizerCaptureSource.start()` directly, which (per its own doc comment) runs synchronously and returns on the caller's thread — so if `Visualizer(0)` throws, its `onError` fires **synchronously on that same thread**, straight into `runAudioSimulationEngine()`, a `while` loop with a real blocking `Thread.sleep`. Unwrapped, that's a main-thread freeze/ANR shape matching what Joe described. (The `phone_mic`/`AudioRecordCaptureSource` path was already safe, since that class spawns its own background `Thread` before ever touching `onError`.)
    - **Fix applied:** (1) `RgbControllerViewModel.kt`'s `executeAudioSideEffect()` now wraps the `StartAudioEngine` case in `viewModelScope.launch(Dispatchers.IO) { startAudioRecording(effect.mode) }`, matching its siblings. (2) `runAudioSimulationEngine()` itself was also made dispatch-safe by construction rather than relying on callers to remember: its entire body (including the blocking loop) now runs inside its own `viewModelScope.launch(Dispatchers.IO) { ... }`, so it can never block the calling thread regardless of which of its two call sites (on-device error fallback, phone-mic error fallback) or any future caller reaches it. Its two call sites are unchanged (both already inside `startAudioEngine`'s `onError` callbacks); the function's contract is now "safe to call from any thread," which the doc comment at its definition spells out.
    - **Verification:** `./gradlew :app:compileDebugKotlin` and `./gradlew :app:testDebugUnitTest` both green — full suite still 253 tests (no new tests added; this is a threading fix to existing dispatch/orchestration code, not new pure logic, consistent with how prior wiring-only changes in this file have been handled). No on-device smoke test was possible for this one, since the failure mode is a race-timing-dependent freeze that wasn't reliably reproducible in the first place.
    - **Relationship to the pre-existing "LEDs freezing intermittently" open bug (see "Known open bug" below):** likely a **different issue, not (necessarily) the same one**. The tracked LED-freeze bug is described as BLE-write/watchdog-related (it has its own diagnostic logging around Bluetooth activity, write queues, and watchdogs — see the "LED Freeze Diagnostic Recorder" in Settings → Experimental) and has been open across multiple phases of BLE work without being tied to the audio engine. This fix addresses a distinct, code-read-confirmed defect in the music-sync audio-engine startup path specifically, triggered by a Visualizer-init failure on backend switch — a different mechanism than a BLE write/GATT freeze. They are being tracked as separate until/unless evidence says otherwise; do not conflate them or assume this fix closes the older bug.

15. **Diagnostic capture-methodology fix — DiagnosticLogger tag exclusion, NOT a pipeline behavior change (2026-07-21).** The audio diagnostic capture from item 13 came back incomplete: `DiagnosticLogger`'s ring buffer (`MAX_ENTRIES = 5000` / `MAX_BYTES_ESTIMATE = 2MB`) filled in ~23 seconds of real time because BLE write/watchdog logging (`DeviceWriteManager`/`BleWriteWatchdog`/`BLE` tags, firing roughly every 50ms per connected device — write-enqueue, write-initiated, and a scheduled+fired watchdog tick per write) accounted for over 90% of entries, evicting both `BackendInfo` one-shot logs and the entire `phone_mic` portion of the session before Joe exported. Only 43 `BeatConfidence` samples survived (all from the VISUALIZER/on-device tail). **This was purely a capacity/priority problem — the instrumentation itself (item 13) was confirmed correct and untouched.**
    - **Fix:** `DiagnosticLogger.start()` now takes an optional `excludedTags: Set<String> = emptySet()` param; `DiagnosticLogger.log()` early-returns if `tag in excludedTags`. Recording behaves exactly as before when the set is empty (default), so the existing BLE-freeze-diagnostic use case (which needs those exact tags) is unaffected unless explicitly opted into exclusion. `MAX_ENTRIES`/`MAX_BYTES_ESTIMATE` were left unchanged — a tag filter was judged the smaller, safer change versus growing the buffer, since the BLE logs are pure noise for an audio-only session rather than something worth keeping more of.
    - **UI:** `SettingsTabContent.kt`'s existing "LED Freeze Diagnostic Recorder" card (Settings → Experimental) gained one new checkbox, "Exclude BLE write/watchdog logs (for audio diagnostics...)", directly above the Start/Stop button. Unchecked (default) preserves the original LED-freeze-diagnostic behavior exactly. Checked, `Start Diagnostic Recording` calls `DiagnosticLogger.start(setOf("DeviceWriteManager", "BleWriteWatchdog", "BLE"))`. The checkbox is disabled while a recording is in progress (`enabled = !isDiagnosticRecording`) so the exclusion set can't change mid-session. No other Settings-tab flow changed — same button, same export/share flow.
    - **Verification:** `./gradlew :app:compileDebugKotlin` and `./gradlew :app:testDebugUnitTest` both green, full 253-test suite unaffected (no new tests — this is throwaway diagnostic tooling per the task's own framing, not production logic warranting new coverage). No on-device smoke test done for this one; the next real audio-diagnostic capture attempt (see below) doubles as verification.
    - **Updated capture steps for the next attempt** (supersedes step 2 of item 13's instructions; steps 1, 3-8 unchanged): at step 2, after tapping to expand **"LED Freeze Diagnostic Recorder"**, **check the new "Exclude BLE write/watchdog logs" checkbox** before tapping **"Start Diagnostic Recording"**. Everything else — Music tab → Phone Mic ~60-90s → stop → On Device ~60-90s → stop → Settings → Stop Diagnostic Recording → Export Log & Share — is identical to before. This should let the full phone_mic + on_device session (both `BackendInfo` one-shot lines and all `BeatConfidence` samples across the whole session, not just the tail) survive to export without needing to touch `MAX_ENTRIES`. If the buffer still fills before both backends are captured, the isolated next step is to bump `MAX_ENTRIES`/`MAX_BYTES_ESTIMATE` in `DiagnosticLogger.kt` — not to re-expand the exclusion set, since `AudioCapture`/`BeatConfidence` are exactly what needs to survive.

16. **Beat-detection feel/tuning pass — confidence wired into flash intensity, strength's dynamic range restored (2026-07-21).** Not a bug fix — the pipeline was working as designed. This is a deliberate tuning pass aimed at making the visualizer feel more responsive/satisfying, backed by the real on-device `BeatConfidence` diagnostic data gathered in items 13/15 (confidence spans 0.31-1.0 across true beats but was being discarded entirely; strength hit its 1.0 ceiling on ~48% of true beats on both backends). **Validated by build + full test suite only — nobody has looked at this running on a device yet. How it actually feels is Joe's call once he builds and tries it, not something confirmed here.**
    - **Fix 1 (`AudioDspProcessor.kt`, `process()`, the `isBeat` block):** `beatPulsePeak` used to be `0.6f + 0.4f * result.strength`, ignoring `result.confidence` (BeatDetector's peak-salience + BPM-lock + phase-grid-agreement score) entirely. Changed to `0.6f + 0.4f * result.strength * result.confidence.coerceIn(0f, 1f)` — confidence now multiplies the strength contribution rather than sitting unused, so a strong-but-uncertain beat (pre-tempo-lock, or off the expected rhythmic grid) still registers but visibly softer than an equally strong, high-confidence one. The 0.6 floor is untouched — every detected beat still gets a minimum visible pulse; only the variable 0.4 portion is confidence-gated.
    - **Fix 2 (`BeatDetector.kt`, `process()`, strength calculation):** `strength` used to be `((evalSample.flux - medianFlux) / scaledMad).coerceIn(0f, 4f) / 4f` — a hard linear ceiling at ratio 4, which is exactly what was pinning ~48% of real beats to an identical 1.0 on real music. Replaced with a soft-saturating curve, `(1f - exp(-ratio / 6f)).coerceIn(0f, 1f)` where `ratio` is the same `(flux - median) / scaledMad` value floored at 0 — this has no hard ceiling at all; it approaches 1 asymptotically, so strength keeps distinguishing "solid beat" from "huge beat" well past where the old formula would have already clipped. Chosen over just widening the old linear ceiling (e.g. `coerceIn(0f, 8f) / 8f`) because a hard ceiling anywhere still eventually saturates on loud enough music — the asymptotic curve doesn't reintroduce the same failure mode at a higher threshold.
    - **Why these two were done together, not separately:** fix 2 changes what range of `strength` values fix 1 receives, and fix 1's multiplicative confidence gating is specifically what prevents a now-uncapped high-strength-but-low-confidence beat from still flashing at max intensity (a low-confidence beat with `strength` near 1 now produces `beatPulsePeak` well below 1.0, e.g. `strength=0.95, confidence=0.35` → `beatPulsePeak ≈ 0.933`, versus `strength=0.95, confidence=0.95` → `beatPulsePeak ≈ 0.961` — sanity-checked by hand against the formula, not just asserted).
    - **Verification:** `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` both green, full 253-test suite unaffected (no new tests — no existing test asserts an exact `strength`/`confidence`/`beatPulsePeak` value, only relative/threshold behavior like "brightness rises above silence on a real beat," which both formulas still satisfy). **No on-device confirmation that this actually feels better** — that's the explicit next step, not done here.
    - **Not in scope, explicitly parked for later:** the sample-rate/bin mismatch between backends (confirmed real per item 13, deliberately deprioritized), latency compensation, and the hue/palette-cycling design question.

17. **Bluetooth delay calibration double-count fix, and a near-silence false-beat fix (2026-07-21).** Two independent bug fixes, both in the audio-sync pipeline, done together in one pass. **Validated by build + full test suite only — neither has been confirmed on a real device yet.**
    - **Bug 1 — calibration's 180ms lookahead was being added on top of, not accounted for by, the tap-to-sync flow.** `totalVisualDelayMs` was computed everywhere as `bluetoothDelayMs + BeatDetector's lookaheadMs (180ms)`. But the calibration flash (`CalibrationSideEffect.SendFlashPulse`, fired from `RgbIntent.SendCalibrationFlash`) is sent via `broadcastCommandDirect` — it bypasses `queueCommand()` and its `totalVisualDelayMs` delay entirely. So the value the user tunes via the metronome-click tap-to-sync UI (`calibrationDelayOffsetMs`, saved as `bluetoothDelayMs`) already **is** the full delay that feels in sync — no queueCommand delay was ever in that loop to begin with. Adding the 180ms lookahead on top in `queueCommand`'s real playback path meant every real music-sync flash landed ~180ms later than what calibration had verified was correct. **Fix:** `totalVisualDelayMs` now mirrors `bluetoothDelayMs` directly (no `+ 180`) at every site that derived it — `RgbControllerViewModel.kt`'s prefs-load in `init`, `onBluetoothAudioRouteActive`/`onBluetoothAudioRouteInactive`, `CalibrationFlowReducer.kt`'s `SaveCalibrationAndExit`/`ResetCalibrationSettings`, and `AudioSettingsReducer.kt`'s `SetBluetoothDelayMs`/`ResetAudioPipelineSettings` — plus `RgbUiState`'s own default (180 → 0). The now-unused `BeatDetector` import was removed from `RgbControllerViewModel.kt`, and the now-unused `BEAT_DETECTOR_DEFAULT_LOOKAHEAD_MS` local constants were removed from both reducers. **Not touched, per scope:** making `lookaheadMs` configurable, and per-device pref keying beyond what already existed (`getCalibrationDelayPrefInt(deviceKey, ...)` is unchanged).
    - **Bug 2 — near-silence could register as a normal beat because the MAD-based threshold normalizes against itself.** Investigated `BeatDetector.kt`'s adaptive threshold: `scaledMad` already has a noise-protection floor (`maxOf(mad * 1.4826f, 0.1f)`) and the absolute candidate threshold already floors at `0.3f` regardless of how quiet things get — so detection was never *purely* relative in the sense of shrinking to zero. But those floors are small, fixed constants, not tied to what "quiet" vs. "loud" actually means for the current listening session. The real gap: the moment a track ends, `mad`/`scaledMad` collapse toward their floor within roughly one flux-history window (~1s), so a strength ratio computed as `(flux - median) / scaledMad` gets divided by a now-tiny number — meaning ambient mic/room noise between tracks, despite being nowhere near the music's actual dynamic range, can look like a completely ordinary beat relative to its own (collapsed) recent variability. **Fix:** added `madReference`, a slow-release EMA of `scaledMad` (tracks upward instantly on any louder frame, decays only ~20%/second — `madReference * 0.995f + scaledMad * 0.005f` per frame — otherwise) inside `BeatDetector`. `effectiveMad = maxOf(scaledMad, madReference * 0.25f)` replaces `scaledMad` in both the candidate threshold and the strength-ratio calculation. This keeps the threshold/normalization anchored to the track's recent loudness for a few seconds after it ends, rather than re-normalizing against near-zero variance immediately — while still letting the detector adapt down over tens of seconds for genuinely quiet songs (25% floor, not 100%). The soft-saturating strength curve from item 16 (`1f - exp(-ratio / 6f)`) is untouched — only the ratio's denominator changed, so item 16's fix is not regressed. Item 16's confidence-gated `beatPulsePeak` in `AudioDspProcessor.kt` is untouched entirely (different file, reads `result.strength`/`result.confidence` as opaque outputs).
    - **Verification:** `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` both green, full 253-test suite. Five existing tests asserted the old `+ 180ms` derivation (`AudioSettingsReducerTest.kt`'s `setBluetoothDelayMs_derivesTotalVisualDelayAsValuePlus180msLookahead`/`_zero_totalVisualDelayEqualsBareLookahead`/`resetAudioPipelineSettings_restoresAllDefaultsIncludingTotalVisualDelay`, `CalibrationFlowReducerTest.kt`'s `saveCalibrationAndExit_withKnownDevice_savesUnderDeviceIdentifier`/`resetCalibrationSettings_resetsStateFieldsAcrossSlices`) and were updated to assert the corrected values, not deleted. No new tests added for the `BeatDetector` MAD-reference change — no existing `BeatDetectorTest`/`AudioDspProcessorTest` assertion depends on the exact `scaledMad` value, only on relative/threshold beat-or-no-beat outcomes, which the change preserves for genuine in-song beats.
    - **Not done here, flagged for next time:** on-device confirmation of both fixes (does the flash now land exactly on the tap-synced beat; does silence between tracks actually stop flashing). Also not done: analyzing the still-pending exported diagnostic log from items 13/15 — that could give a real number for how long post-track ambient noise stays elevated, which would let `madReference`'s 0.995/0.25 constants be tuned from data instead of a reasonable-guess starting point.

18. **AGP bumped to 9.3.0, Gradle parallel sync enabled (2026-07-21).** Housekeeping, unrelated to the audio-pipeline work above. `gradle/libs.versions.toml`: `agp` `9.1.1` → `9.3.0`. `gradle.properties`: added `org.gradle.tooling.parallel=true` (enables parallel sync for Gradle 9.4+). Neither is documented further — applied via Android Studio tooling, not diagnosed as fixing any specific issue.

19. **Audio→LED mapping-layer rewrite, stage 1 of 4 — time-basing + `flashFloor`/`flashRange` (2026-07-21).** First slice of `mapping-proposal-audio-to-led-2026-07-21.md`'s §6 staged plan (a full design doc for restructuring the hue/saturation/value mapping layer, not yet committed to this file's numbered history until it starts landing — see that file for the full anchor+breath rationale). Branch `mapping-stage1-time-basing-flash-params`, off `main`. **Deliberately scoped to be small and low-risk, per the plan's own staging: no anchor/breath rewrite yet (that's stage 2), `BeatDetector` untouched, backend-branched `baseSat` coefficients untouched, deferred DSP sample-count window constants untouched — all per the proposal's §7 out-of-scope list.**
    - **`flashFloor`/`flashRange`:** `AudioDspProcessor.kt`'s beat-flash-peak formula was hard-coded as `beatPulsePeak = 0.6f + 0.4f * strength * confidence` (the item-16 formula). Split the `0.6f`/`0.4f` into two new `AudioSettingsState` fields (`flashFloor`, `flashRange`, both defaulting to the old constants) so a later stage's per-preset table (proposal §5) has somewhere to write to. This stage does **not** populate that table yet — `visualizerConfigFor()` in `AudioSettingsReducer.kt` gives every preset the same `0.6f`/`0.4f` values, so `SetVisualizerPreset`/`ResetAudioPipelineSettings` are wired end-to-end (state + `flash_floor`/`flash_range` prefs persistence + `RgbControllerViewModel`'s prefs-load in `init`) but produce zero visual change. The formula *shape* is unchanged, only parameterized.
    - **Time-basing:** `AudioDspProcessor` gained a `lastFrameNowMs` field and derives `dtMs = nowMs - lastFrameNowMs` (0 on a run's first frame) at the top of `process()`. Applied to exactly one existing mechanism: `whiteHotFlashOffset`'s recovery, previously a fixed `+0.05f` per frame (≈20 frames to fully recover), which meant ~465ms on the ~43Hz AudioRecord backend vs. ~1000ms on the ~20Hz Visualizer backend for what was supposed to be the same recovery — this is structural problem #2 from the proposal doc. Now `whiteHotFlashOffset += dtMs / WHITE_FLASH_RECOVERY_MS` (a new private constant, 1000f, chosen to match the Visualizer backend's original effective duration). **This is a real, intentional behavior change on the `phone_mic`/AudioRecord backend** (its white-flash recovery is now ~2.15× slower, matching Visualizer) — flagged explicitly for the on-device smoke test below, not something to assume is unnoticeable.
    - **Deliberately NOT time-based in this stage, even though the proposal's problem #2 names them too:** `continuousHueOffset`'s per-frame drift (`+= activeTotal * 0.01f`) and `beatHueOffset`'s per-frame decay (`*= 0.85f`). Both are slated for deletion in stage 2's anchor+breath rewrite (§4 of the proposal), so time-basing them now would be thrown-away work.
    - **Tests:** `AudioSettingsReducerTest.kt`'s `setVisualizerPreset_punchy_appliesAllSixteenFields` and `resetAudioPipelineSettings_restoresAllDefaultsIncludingTotalVisualDelayIsZero` updated for the two new fields/effects (preset test's effect count 18 → 20). New `AudioDspProcessorTest.kt` test `flashFloor and flashRange settings scale the beat flash peak` — drives a real beat through the same synthetic-transient pattern as the existing Beat Only test, once with `flashFloor=flashRange=0f` and once with defaults, asserting the zeroed run produces a lower peak — proves the fields are actually consumed, not just plumbed. No test added for the `dtMs`-based recovery timing itself (would need real elapsed-wall-clock assertions across frames; the existing test suite doesn't do that anywhere, and the two-call-site consumers of `whiteHotFlashOffset`/`sat` already have coverage of the value pipeline generally). `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` green, full suite 253 → 254 tests.
    - **On-device smoke test explicitly deferred, by Joe's choice — not an oversight.** The original plan gated stage 2 on smoke-testing all 8 presets/both backends here (specifically the `phone_mic` white-flash recovery-rate change). Joe chose instead to run one on-device pass after all four stages land, not after each one — same precedent as the color-conversion dedup (item 10). This means stages 2-4 build on an unverified stage 1; if the on-device pass at the end turns up a problem traceable to stage 1 (e.g. `phone_mic` white-flash recovery reading as sluggish), **that's the first thing to isolate and re-check**, since nothing after this point has confirmed it's fine.

20. **Audio→LED mapping-layer rewrite, stage 2 of 4 — the anchor+breath hue rewrite + full §5 per-preset table (2026-07-21).** Same branch as stage 1, `mapping-stage1-time-basing-flash-params` (not renamed — both stages are small enough to stay on one branch pending the deferred smoke test, see item 19). This is the core structural change the proposal doc's §4 describes. `BeatDetector` internals, the backend-branched `baseSat` coefficients, and the deferred DSP sample-count window constants remain untouched, per the proposal's §7 scope boundary — verified by re-reading current source before starting, not assumed from the doc's own claims.
    - **Deleted:** `continuousHueOffset` (the unbounded per-frame drift + per-beat +30°/+15° jump accumulator), `beatHueOffset` (the 180° decaying impulse), the binary `isSustainedSection` +120° hue toggle, and the `currentPaletteIndex`/`palettes` fixed 6-color-cycle array. All four were structural problems named in the proposal doc (problems #1, #3, #4, #5) — none were preserved-as-quirks the way earlier phases' "faithful bugs" were, because the proposal doc is itself the source of truth being implemented here, not a prior behavior to keep byte-identical to.
    - **Added — 11 new `AudioSettingsState` fields** (on top of stage 1's `flashFloor`/`flashRange`): `anchorBeatsPerAdvance`, `anchorTimerMs`, `hueAnchorJumpDeg`, `hueJumpConfidenceGate`, `hueBreathRangeDeg`, `breathUsesBassRatio`, `hueDriftDegPerSec`, `hueDegreesPerBeat`, `sustainResponse` (string enum: `"NONE"`/`"HUE_SHIFT"`/`"SAT_BOOST"`/`"BRIGHTNESS_SWELL"`, matching the existing `visualizerPreset`/`musicMode` string-enum convention rather than introducing a new sealed type), `sustainRampMs`, `whiteFlashRecoveryMs` (replaces stage 1's single global `WHITE_FLASH_RECOVERY_MS` constant with a per-preset value). Wired through `AudioSettingsReducer.kt`'s `VisualizerConfig`/`visualizerConfigFor()`/`SetVisualizerPreset`/`ResetAudioPipelineSettings` and `RgbControllerViewModel`'s prefs-load in `init`, same pattern as every existing preset-table field.
    - **`AudioDspProcessor.kt`'s hue pipeline, rewritten around `hueAnchor` (discrete, moves only on qualifying events) + `breath` (bounded tilt, recomputed fresh every frame, never accumulates):**
      - Anchor moves are confidence-gated: on a beat, `beatsSinceAnchorMove` only increments when `result.confidence.coerceIn(0f,1f) >= state.hueJumpConfidenceGate`; once it reaches `anchorBeatsPerAdvance`, the anchor jumps by `hueAnchorJumpDeg` and resets the counter. `anchorBeatsPerAdvance = 0` means never (Smooth Flow).
      - A new `anchorTimerMs` fallback (not a named §5 column — needed to implement Ambient Chill's documented "every 16 beats, or a 30s timer if no BPM lock" — only Ambient Chill sets it nonzero) fires an anchor move on elapsed wall time when `bpmConfidence < 0.3f`, so presets that want color variety aren't stuck if the detector never locks a tempo.
      - Continuous drift is folded directly into `hueAnchor` (`hueAnchor += driftDegPerSec * dtSec`), not a separate accumulator — per the proposal's explicit mechanics note ("drift rotates the anchor's reference frame... anchor += drift·dt"), so it can never run away independently of the discrete color state the way `continuousHueOffset` used to. `driftDegPerSec` crossfades `hueDriftDegPerSec` (free-run) with `(bpm/60) * hueDegreesPerBeat` (tempo-locked) by `bpmConfidence` — Smooth Flow is the only preset using the tempo-locked term (`hueDegreesPerBeat = 1.5f`).
      - `breath = ((midRatio - highRatio) * hueBreathRangeDeg).coerceIn(-range, range)` by default — the existing spectral-tilt formula, now bounded per preset instead of a hard-coded ±60°. Bass Thump sets `breathUsesBassRatio = true`, keying breath to `bassRatio` instead via a centered-on-⅓ mapping — **this mapping formula is a judgment call, not specified numerically in the proposal doc** (the doc only says "breathing keyed to bassRatio... its low end"), flagged in-code and here for anyone tuning it later.
      - `hue = (hueAnchor + breath) % 360`. The EMA circular-smoothing snap now fires only when `anchorMovedThisFrame` is true, not on every raw beat as before — this is what lets Ambient Chill (anchor advances rarely) breathe continuously without ever hard-cutting, per proposal problem #3.
      - White-flash desaturation depth is now analog: `whiteHotFlashOffset = 1f - (strength * confidence).coerceIn(0f,1f)` on a qualifying white-flash-preset beat, replacing the old binary `= 0f`. Recovery now reads `state.whiteFlashRecoveryMs` per preset instead of stage 1's single global constant (Strobe Blast 250ms, Laser Sharp 120ms, Beat Only 250ms; everything else defaults to stage 1's 1000ms, functionally unused since they never zero the offset).
      - Sustain response replaces the old binary condition+toggle with **hysteresis** (enter: `shortMean > threshold && rollingVariance > 5` held ≥400ms; exit: a new, separate `shortMean < threshold * 0.8` condition — the original had no distinct exit condition at all — held ≥800ms) and a **ramp** (`sustainIntensity` 0↔1 over `sustainRampMs`, applied as a multiplier on the response's magnitude). Four responses, selected per preset: `HUE_SHIFT` (retains the old 120° magnitude, now ramped/debounced instead of an instant toggle), `SAT_BOOST` (**+0.3f magnitude is a judgment call** — the proposal doc gives no numeric value for this response, only naming it), `BRIGHTNESS_SWELL` (+0.10f, the one magnitude the doc does give explicitly, for Ambient Chill), `NONE`.
    - **`isPaletteCyclingEnabled` scope decision, made during implementation, not specified by the doc:** the field, its `SetPaletteCyclingEnabled` intent, and its UI toggle are all left completely alone — but `AudioDspProcessor` no longer reads it at all. The mechanism it used to gate (advancing through the fixed 6-color `palettes` array) is retired outright, superseded structurally by the anchor system for every preset — not repurposed to gate anchor advancement, because Beat Only's own §5 row explicitly keeps per-beat anchor jumps (`hueAnchorJumpDeg = 137.5f`, golden angle) while `isPaletteCyclingEnabled = false` for that preset, which rules out "palette cycling off ⇒ anchor frozen" as a valid mapping. The toggle is now a vestigial, still-persisted pref with no effect on hue — flagged here explicitly rather than silently left inconsistent, in case a future UI pass wants to either repurpose or remove it.
    - **Full §5 per-preset table applied** in `visualizerConfigFor()` (`AudioSettingsReducer.kt`) — see that function for the concrete numbers; matches the proposal doc's table with one addition (`anchorTimerMs`, Ambient Chill only) not in the doc's own column list.
    - **Tests:** two new `AudioDspProcessorTest.kt` cases isolate the anchor mechanism from breath/drift/sustain by zeroing those out via settings overrides — `a qualifying beat advances the hue anchor by hueAnchorJumpDeg` and `a beat below hueJumpConfidenceGate flashes but never moves the hue anchor` (the latter initially failed against the *test's* own setup, not production code — the default `sustainResponse = "HUE_SHIFT"` was independently perturbing hue during the synthetic transient; fixed by setting `sustainResponse = "NONE"` in both isolation tests, not by changing `AudioDspProcessor`). `AudioSettingsReducerTest.kt`'s Punchy preset-table test extended with the 11 new field assertions and effect count (20 → 31). No new hysteresis/ramp-timing test — would need real elapsed-wall-clock assertions across many frames, consistent with stage 1's white-flash-recovery timing not getting one either. `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` green, full suite 254 → 256 tests.
    - **Not done here:** on-device smoke test (deferred with stage 1, see item 19's note — now covers stages 1, 2, and 4, see item 21 below). Also not done: any UI surface for the 11 new per-field knobs (no `SetHueAnchorJumpDeg`-style intents, no Settings-tab sliders) — they're only reachable via preset selection right now, matching how this stage's own §6 implementation-surface section scoped it (preset table + `AudioDspProcessor` rewrite only, no new UI mentioned).
    - **Stage 3 skipped as a separate step — already fully covered by this stage.** The proposal's §6 staging plan listed "tempo-lock + bar counting (`anchorBeatsPerAdvance`, `hueDegreesPerBeat`)" as its own stage 3, after the anchor+breath rewrite. But implementing "the full §5 per-preset table" (this stage's own stated deliverable) necessarily required real per-preset values for both of those fields — Balanced (`anchorBeatsPerAdvance=2`), Ambient Chill (`=16`, plus its timer fallback), and Smooth Flow (`hueDegreesPerBeat=1.5`, the tempo-locked-drift showcase) all depend on them. There is nothing left in `AudioDspProcessor`/the reducer that a separate stage 3 pass would have added; confirmed with the user before proceeding straight to stage 4.

21. **Audio→LED mapping-layer rewrite, stage 4 of 4 — anticipatory dimming via `nextPredictedBeatMs` (2026-07-21).** Same branch, `mapping-stage1-time-basing-flash-params`. The proposal doc (§4) flags this stage as optional/opt-in, listed separately from the "core proposal" specifically because it's the one slice that touches `BeatDetector.kt`'s output surface rather than staying purely in the mapping layer — user confirmed proceeding rather than stopping at stage 2/3.
    - **`BeatDetector.kt`: one new field, zero change to detection/tuning math.** `BeatResult` gains `nextPredictedBeatMs: Long? = null` (default keeps every existing call site — including the three early-return `BeatResult(isBeat = false, ...)` calls scattered through `process()` — compiling unchanged). Computed once, at the bottom of `process()`, by extrapolating forward from `gridBeats.last()` (the existing DP beat-grid field, previously computed but never exposed outside the class) in whole `60000f / lockedBpm` periods until past `now` — necessary because `gridBeats` is only rebuilt every `tempoUpdateIntervalMs` (1500ms) via `updateTempoAndGrid()`, so it can lag behind the current frame between rebuilds. Bounded to 64 iterations against a stale/degenerate grid. `null` whenever there's no tempo lock or no grid yet (same gating condition `phaseAgreement`'s existing calculation already uses a few lines above).
    - **`AudioDspProcessor.kt`: the pre-dip, a new *consumer* of that field.** In the non-`Beat Only` branch of the value calculation, `ambientLevel` is now multiplied by `(1f - preDip)`, where `preDip` ramps `0 -> 0.10f` linearly over the 80ms leading into `result.nextPredictedBeatMs` and is `0f` everywhere else (no lock, or outside the window). Applied only to the ambient contribution, not the beat-flash envelope itself — the effect is a brief dip right before a predicted beat, making the actual flash (whenever `isBeat` fires) read as a bigger jump by contrast. `Beat Only`'s branch is untouched (it has no ambient term to dip). No new `AudioSettingsState` field or per-preset knob — the proposal's own text frames this as a mechanism, not a per-preset tunable, so it applies uniformly whenever a tempo lock exists, same as `phaseAgreement`'s confidence contribution already does.
    - **Tests:** new `BeatDetectorTest.kt` (the class had zero test coverage before this — not a gap introduced by this stage, a pre-existing one) with one test, `nextPredictedBeatMs is null before any tempo lock`, covering only the new field's default/no-lock behavior. **Driving a real synthetic tempo lock to test a non-null `nextPredictedBeatMs` value, or the pre-dip's actual effect on `AudioDspProcessor`'s output, was not attempted** — doing so realistically needs several seconds of periodic synthetic beats through the full autocorrelation/DP-grid pipeline (`tempoUpdateIntervalMs` alone is 1500ms), which is a materially bigger testing investment than anything else in this mapping-layer work; consistent with stage 2's hysteresis/ramp-timing and stage 1's white-flash-recovery timing also going untested for the same reason (would need real elapsed-wall-clock assertions across many frames). `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` green, full suite 256 → 257 tests.
    - **Not done here:** on-device smoke test (deferred with stages 1-2, see item 19/20's notes — this is the third and final stage folded into that same deferred pass, per the user's explicit "skip smoke test, will do smoke test after all stages" instruction). Whether the pre-dip is actually perceptible/worth keeping — as opposed to just correct-per-spec — is exactly the kind of judgment call that on-device pass is for.

22. **Post-mapping-layer follow-ups from the deferred smoke test: pre-dip gate fix, Smooth Flow brightness tuning, on-device first-activation fix (2026-07-21).** Same branch, `mapping-stage1-time-basing-flash-params`. Three items surfaced once the deferred smoke test from items 19-21 was actually run.
    - **Pre-dip gate fix (commits `273038c`/`0039fbe`, predates this item's own work but undocumented until now):** the stage-4 anticipatory pre-dip (item 21) was gated on `state.flashRange > 0f`. Since `flashFloor`/`flashRange` default to `0.6f`/`0.4f` for every preset that doesn't explicitly override them (stage 1, item 19) — including Ambient Chill and Smooth Flow, whose `flash` field is `0.00f` — the gate never actually excluded those two presets from the pre-dip. Fixed to gate on `state.audioFlashStrength > 0f` instead, the actual per-preset "does this preset flash at all" signal (see the in-code comment at `AudioDspProcessor.kt`'s pre-dip block for the full explanation). Confirmed via the smoke test that this fixed Ambient Chill's perceived dimness.
    - **Smooth Flow brightness tuning (`AudioSettingsReducer.kt`):** Smooth Flow was still too dim after the pre-dip fix — diffed against Ambient Chill and confirmed this is original preset design, not a pipeline bug (Smooth Flow's `sustainResponse = "HUE_SHIFT"` never touches `value`, unlike Ambient Chill's `BRIGHTNESS_SWELL`). Raised `ambientCapFraction` (0.45f → 0.65f, the direct multiplicative cap on `AudioDspProcessor`'s ambient brightness term — not auto-gain-normalized away, unlike `bassGain`/`midGain`) and `minBrightness` (0.18f → 0.30f, the hard floor). Initially kept below Ambient Chill's 0.75f/0.35f to preserve Smooth Flow's identity — **confirmed on-device both fixes work, but Smooth Flow was still not bright enough at 0.65f/0.30f**, so both values were pushed a second time to match Ambient Chill's exactly: `ambientCapFraction` 0.65f → **0.75f**, `minBrightness` 0.30f → **0.35f**. Smooth Flow's identity is preserved by its other fields instead (drift/breath hue motion, `beatMult = 1.8f` vs. Ambient Chill's `2.5f`, plus `sustainResponse`/anchor behavior), not by holding these two brightness levers lower — with no beat flash (`flash = 0.00f`, same as Ambient Chill), these two fields are the only brightness knobs a no-flash preset has, so there was no room to keep them below Ambient Chill's values once 0.65f/0.30f proved insufficient. No test pinned Smooth Flow's exact values at either pass, so this needed no test updates. `flash`/pre-dip logic untouched, per scope.
    - **On-device first-activation fix (`VisualizerCaptureSource.kt`) — source-level fix for a well-documented platform quirk, NOT confirmed against a real Logcat trace.** Reported repro: select on-device backend → first click does nothing → toggle off → toggle on again → now it works. **No device/adb was available in the environment this fix was written in — could not capture a live trace, flagged explicitly, same as item 14's precedent.** Root-cause investigation ruled out a same-intent ordering bug (`StopMusicSyncInternal` always fully completes synchronously before `StartAudioEngine`'s effect is even processed) and found no DSP-side warm-up gate (`AudioDspProcessor.process()` has exactly one early-return, `numBins < 349`, not a multi-frame warm-up). Leading hypothesis, grounded in known `Visualizer(session=0)` platform behavior: session 0 (the device's global output mix) can attach and report success (`enabled = true`, no exception) while silently receiving zero `onFftDataCapture` callbacks if attached at a moment the output mix hasn't stabilized — `VisualizerCaptureSource` never verified data was actually flowing. Manually toggling off/on works around this by releasing and reconstructing the `Visualizer`. **Fix:** `VisualizerCaptureSource.start()` now spawns a one-shot daemon watchdog thread (`WATCHDOG_TIMEOUT_MS = 1500L`) after `enabled = true`; if zero frames have arrived by the timeout and capture is still supposed to be running, it tears down and reconstructs the `Visualizer` exactly once (guarded by a `retried` flag — cannot loop), i.e. automates exactly what the manual toggle already did. Logged via `DiagnosticLogger`'s existing `"AudioCapture"` tag so a real on-device capture (per item 13's methodology) will show whether this watchdog actually fires, which would be the first real evidence either confirming or ruling out this hypothesis. Also fixed as a genuine, independently-justified correctness bug found during this investigation (real, regardless of whether it's this symptom's cause): `RgbControllerViewModel`'s `audioCaptureSource`/`audioDspProcessor`/`transmissionThread` fields were plain (non-`@Volatile`) vars written by `startAudioEngine()` (runs on `Dispatchers.IO`, per item 14's fix) and read/nulled by `stopMusicSyncInternal()` (runs synchronously on the calling thread) — a real JMM cross-thread visibility hazard with no happens-before edge between the two. Now `@Volatile`.
    - **Tests:** one new `AudioCaptureSourceTest.kt` case, `VisualizerCaptureSource watchdog timeout is a small, bounded positive window`, asserting `WATCHDOG_TIMEOUT_MS` stays in a sane 500ms-5000ms range — construction/contract-level only, consistent with this test file's existing philosophy (Robolectric doesn't model real audio input, so the watchdog's actual reconstruct-on-silence behavior is untestable here). `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` green, full suite 257 → 258 tests.
    - **Not done here:** on-device confirmation that either fix actually resolves the reported symptom. Both are explicitly flagged as unconfirmed-by-trace in the fix descriptions above — treat as ruled out and look elsewhere if the on-device backend still fails to activate on first click after this.

23. **Delivery-path fixes from `visualizer-review-2026-07-21.md`: P0 latency instrumentation, P2 peak-survival, P4 multi-device spatial roles — COMPLETE, merged to `main` via PR #24 (merge commit `e76f53a`, 2026-07-21). P1 (predictive flash scheduling), P3 (macro-dynamics), P5 (saturation) explicitly deferred, per that doc's own sequencing.** Branch `visualizer-delivery-fixes`, off `main`, left undeleted per standing convention. This phase was gated on an on-device smoke test before merging, for two specific reasons neither unit test can exercise, both now **confirmed done by the developer**:
    1. **The DB bump (v4→v5) is destructive, not additive.** `SavedDevice` gaining `deviceRole`/`hueOffsetDegrees` relies on `AppDatabase`'s existing `fallbackToDestructiveMigration()` — same mechanism every prior version bump in this table has used — but that means every real device's saved-devices table (auto-connect flags, active-control flags, aliases-adjacent state) gets wiped and recreated empty on first launch after this update, not migrated. Confirmed acceptable on a real installed copy of the app (not a fresh emulator) before merging.
    2. **The write-path change touches real BLE timing.** `DeviceWriteManager`'s peak-hold/pacing-bypass logic changes when and whether a command actually reaches the wire under real pacing/backpressure — Robolectric/JVM tests can assert the queue logic in isolation but cannot exercise real GATT write latency, real firmware response to a bypassed pacing wait, or whether peak-hold ever stalls a lower-priority command longer than intended against a real device. Confirmed against real hardware.
    `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` both green, 258 tests (unchanged — no new tests added this pass, see below).
    - **P0 (`DeviceWriteManager.kt`, `AudioDspProcessor.kt`, `RgbControllerViewModel.kt`):** the detection/enqueue/write-initiated timestamps the review asked for already existed as separate `DiagnosticLogger` lines (each line is self-timestamped); what was missing was a way to correlate them. Added: an `"AudioSync"` log in `publishAudioDspResult` on the exact beat-detection frame, carrying the same cmdHex `DeviceWriteManager`'s existing `"Write enqueued"` log already emits (exact match only for Mirror-role devices, the common case — a role-transformed device's actual bytes differ, a known limitation, not a bug). Added cmdHex to the previously-hex-less `"writeCharacteristic() initiated"`/`"returned false"` log lines so the full chain is joinable by hex. Added a `"Pacing settled"` log to `DeviceWriteManager.onWriteCompleted()`'s existing ~1s fps-update cadence, logging `currentPacingMs` per device for free.
    - **P2 (`DeviceWriteManager.kt`, `BleGattTransport.kt`, `AudioDspProcessor.kt`, `RgbControllerViewModel.kt`):** `DeviceWriteManager.updateCommand` gained `priority: Float`/`bypassPacing: Boolean` params (both default 0f/false, so every non-audio caller — calibration flashes via `PacingAutoTuneDialog`, general `broadcastCommand`/`sendCommand` — is unaffected). The same-type dedupe-by-latest rule now only removes a queued command if the new one's priority is `>=` the existing one's; a higher-priority (bigger flash) command that hasn't been written yet survives a lower-priority frame trying to overwrite it. `tryWrite()` skips the pacing-wait gate entirely when the head-of-queue command is `bypassPacing`. `BleGattTransport.writeCommand`/`getPacingMs` thread this through (interface + Android impl + `FakeBleGattTransport`). `AudioDspProcessor.process()` gained an `effectivePacingMs: Int = 0` param; when nonzero, `effectiveBeatFlashDecayMs = maxOf(state.beatFlashDecayMs, effectivePacingMs * 2.5f)` replaces `state.beatFlashDecayMs` in both the Beat-Only and normal-branch envelope calculations, so a short preset decay (Laser Sharp's 70ms) can't fall entirely between two paced writes at slower pacing. `RgbControllerViewModel` computes this as the max `currentPacingMs` across currently-controlled devices (`currentEffectivePacingMs()`) — the slowest connected device is the binding constraint. The pre-dip retarget (bundled per the review's explicit ask) changed `msUntilPredictedBeat` from `nextPredictedBeatMs - nowMs` to `(nextPredictedBeatMs + beatDetector.lookaheadMs) - nowMs`, so the dip now targets the predicted *flash* time (post-detection-latency) instead of the predicted audible onset — `BeatDetector.lookaheadMs` is a public val, no `BeatDetector` change needed.
    - **P4 (`db/Entities.kt`, `db/AppDatabase.kt` v4→v5, `db/RgbDao.kt`, `RgbDatabaseRepository`/Impl, `RgbControllerViewModel.kt`, `DevicesScreen.kt`):** `SavedDevice` gained `deviceRole: String = "Mirror"` and `hueOffsetDegrees: Float = 180f` (string-enum convention, matching `visualizerPreset`/`sustainResponse`; DB version bump relies on the existing `fallbackToDestructiveMigration()`, same as every prior version bump in this table). `AudioDspResult` (`AudioDspProcessor.kt`) gained `sat`/`value`/`ambientValue`/`isBeat`/`bassLevel`/`midHighLevel` fields (all defaulted, so `DemoAudioDspSimulator`'s construction site — unchanged — just gets the defaults, meaning demo-mode devices don't get real role transforms, an accepted scope gap) so the transform has HSV components and per-band levels to work from without re-deriving them from RGB bytes. New `RgbControllerViewModel.broadcastAudioResultDirect(result)` replaces the old identical-bytes-to-every-device broadcast for the audio path only (calibration/general commands still go through the untouched `broadcastCommandDirect`): `HueOffset` recomputes RGB at `(hue + hueOffsetDegrees) % 360`; `AlternatingFlash` round-robins which device(s) in that role get `result.value` vs `result.ambientValue` on each beat via a shared `alternatingFlashBeatCounter % groupSize` (generalizes beyond 2 devices); `BandSplit` splits by index parity among BandSplit-role devices between `bassLevel`/`midHighLevel`. `queueAudioResult`/`broadcastAudioResultDirect` mirror `queueCommand`/`broadcastCommandDirect`'s delay-then-broadcast shape exactly (same `totalVisualDelayMs` handling), just per-device instead of per-broadcast. `setDeviceRole`/`setDeviceHueOffsetDegrees` on the ViewModel go straight to the repository (same as the pre-existing `updateCustomMode`), bypassing `dispatch()`/the reducer system entirely — consistent with `SavedDevice` already living outside `RgbUiState` as its own repository-backed `StateFlow`. UI: `DevicesScreen.kt` gained a "Visualizer Role" `FilterChip` row per saved device (four roles) plus a conditional 0-360° `Slider` for `HueOffset`'s offset, placed in the existing per-device Devices-tab card rather than a literal "Settings" tab screen — a scope call: `DevicesScreen` is where every other per-device control (`Auto-Connect`/`Active Control` toggles) already lives, and this project's own "Settings" usage elsewhere refers to the app-wide Settings tab, not per-device config.
    - **Not done here, explicitly out of scope this pass (per the task's own framing):** P1 predictive/fast-path flash scheduling, P3 macro-dynamics/drop detector, P5 saturation. **No new automated tests** — the peak-hold/pacing-bypass logic has no existing `DeviceWriteManagerTest.kt` to extend (would need a mocked `BluetoothGatt`/`BluetoothGattCharacteristic`, a heavier lift than anything else in this pass) and the role-transform math is straightforward arithmetic already covered in spirit by existing `ColorConverter`/`AudioDspProcessor` coverage; flagged rather than silently skipped, consistent with how this file has always called out untested timing-dependent changes (item 19's white-flash recovery, item 20's hysteresis/ramp timing, item 21's tempo-lock, all went untested for the same "needs real elapsed-wall-clock/hardware" reason).

24. **Predictive flash scheduling (P1), tempo-lock/noise-floor fixes, and the on_device signal-zero fix — COMPLETE, merged to `main` (2026-07-22).** Landed on branch `predictive-flash-scheduling`, left undeleted per standing convention. Covers three rounds of work from the same investigation, now all committed:
    - **P1 predictive scheduling + Flash Timing Offset slider** (`AudioSettingsState.flashTimingOffsetMs`, 0-300ms, default 100). Schedules the beat flash ahead of the predicted beat rather than reacting to it — see item 21's `nextPredictedBeatMs` mechanics.
    - **Beat-detection tuning:** `minBpm` widened 60→40 (a 60bpm floor put real tempos below it, e.g. a 50bpm metronome test, entirely outside the autocorrelation search range, causing lock onto bogus noise peaks); the tempo lock now drops after ~2 consecutive silent update cycles instead of freezing forever (previously `nextPredictedBeatMs` kept extrapolating off a dead grid indefinitely after playback stopped, so the P1 scheduler kept firing phantom scheduled flashes); the fixed noise-floor constants (`NOISE_FLOOR_MAD_SCALE`/`NOISE_FLOOR_ABSOLUTE`, was `0.1f`/`0.3f`) lowered to `0.07f`/`0.22f` after on-device data showed a confirmed ~120bpm soft passage missing 36-59% of expected beats across four captures; and the causal fast-trigger path now requires `totalEnergy >= noiseGateThreshold` before firing, fixing a phantom-flash bug where ordinary statistical noise during genuine silence routinely crossed its own undamped adaptive threshold.
    - **A still-open, unresolved lead:** a `"BeatGridPhase"` diagnostic log (signed deviation between a confirmed beat's onset and its nearest DP grid node) was added to investigate a separately-reported ~200-250ms early-fire bias in the predicted beat grid itself. Empirically reconfirmed the magnitude (~210-220ms) from pulled captures, but the actual DP-grid root cause (candidate suspects: `transitionPenalty=100f` letting the DP wander within its search window, or real-music onset smearing vs. a metronome's clean transients) is **still not found or fixed** — the log is in place for the next capture. If picking this up: re-test the Flash Timing Offset slider's usable range once it's fixed (right now the slider can only make an already-early flash earlier, never correct it).
    - **The on_device signal-zero bug** (FFT callbacks firing on schedule with correct size but all-zero content for an entire session) was root-caused and fixed — see item 25.
    - Full test suite green (260 tests) throughout; on-device confirmation for the tempo-lock/phantom-flash/noise-floor fixes and for the signal-zero fix specifically is documented in item 25.

25. **on_device backend signal-zero bug: root-caused and fixed via a self-driving adb test harness, confirmed on real hardware (2026-07-22).** Same branch as item 24. `Visualizer(session=0)` could bind to the output mix at a moment the primary output thread was idle/mid-transition, producing a clean, no-exception `enabled=true` object that fired `onFftDataCapture` on schedule and at the right size forever — but with all-zero content, because the binding never actually connected to the stream that started later. Nothing exception-based or Java-observable catches this, which is exactly why manually toggling the on_device card off/on a few times would eventually recover it by luck (a fresh, uncorrelated-timing attach). This is a different failure mode from two earlier leads chased during the same investigation and since ruled out: a silent-attach watchdog gap (item 22, already fixed — that watchdog only checked "did any callback fire," not "does it carry real content") and an `AudioTrack`-session-lifetime hypothesis (tested via a new sustained-playback mode, ruled out — signal was still zero with one long-lived track).
    - **The harness:** `AdbControlReceiver` (debug-only, `BuildConfig.DEBUG`-gated, dynamically registered from `RgbControllerApplication.onCreate()`) exposes `start_diagnostics`/`stop_diagnostics`/`start_metronome[_sustained|_tone]`/`stop_metronome`/`select_backend`/`stop_backend`/`status` over `adb shell am broadcast -a com.example.debug.ACTION_CONTROL -p com.aistudio.expressivergb.xkzqp --es cmd <cmd> [extras]`. `AdbTestMetronome`/`MetronomePlayer` play synthetic click tracks or a continuous tone through real `STREAM_MUSIC` output (not a mock), so `phone_mic` and `on_device` can be driven identically without a human tapping buttons. `AdbControlSink` bridges the receiver to the live `RgbControllerViewModel` via a single-listener slot (same no-DI-path pattern as `AmbianceCommandSink`). Kept in the tree deliberately (not stripped before merge) — useful for any future audio-pipeline investigation.
    - **Non-obvious gotcha worth keeping in mind if extending the harness:** its original test tone used the legacy `AudioTrack(streamType, ...)` constructor with a minimum-sized buffer, which qualifies for Android's low-latency **FastMixer/FastTrack** output path (confirmed via `adb shell dumpsys media.audio_flinger` — an active `FastMixer` thread, `AUDIO_OUTPUT_FLAG_FAST`, and `0 Effect Chains` on every output thread). FastTrack audio is mixed on a dedicated thread that bypasses the Normal mixer thread's effect chain entirely — the exact thread `Visualizer(session=0)` taps — so a fast-tracked test tone is structurally invisible to it regardless of retries, a completely different (and irrelevant) failure mode from a real bug. This silently invalidated early captures made with this harness. Fixed by building tracks via `AudioTrack.Builder()` with explicit `AudioAttributes` (`USAGE_MEDIA`/`CONTENT_TYPE_MUSIC`) and `PERFORMANCE_MODE_NONE` (both documented FastTrack disqualifiers), plus a buffer several times the platform minimum. Real media apps (ExoPlayer, MediaPlayer) aren't normally fast-tracked, which is why real music behaved differently from the harness's original click track — if a future capture ever looks suspiciously all-zero again, check this before re-opening a platform-restriction theory.
    - **Fix, in `VisualizerCaptureSource.kt`:** added `receivedRealSignal`, set only once a callback's FFT bytes contain a real nonzero value (distinct from the pre-existing `receivedAnyFrame`, which fires regardless of content). The attach watchdog now checks `receivedRealSignal` and retries release+reconstruct up to `MAX_ATTACH_RETRIES` (5) times (was one-shot), falling back to the demo-simulation path via `onError` if real signal never appears — instead of silently leaving a permanently-mute `Visualizer` running and reporting success forever.
    - **Confirmed on real hardware:** an on_device capture against the harness's continuous-tone mode showed 4 reconstruction attempts correctly detected as zero-content, the 5th succeeding with real nonzero FFT bytes, real signal energy, and a locked BPM matching the test tone for the rest of the session; signal correctly reverted to zero once the tone stopped.
    - **Not done here:** whether any real streaming app on this device ever gets fast-tracked in practice (unconfirmed, though Android's own eligibility rules suggest ExoPlayer/MediaPlayer typically aren't); the harness's unrelated `select_backend` crash when fired while the device is locked/backgrounded (`ForegroundServiceStartNotAllowedException`) — still open, resurfaced this session and blocked one capture attempt.

26. **`visualizer-review-2026-07-22.md` bug fixes: flash-fire-frame re-keying (A1/A2/A4), flash-floor scaling (A3), sustain-ramp continuity (A5), shared-session captureSize guard (A6), demo-fallback black-device fix (A7), flash staleness cutoff (B1) — COMPLETE, merged to `main` via item 28 below (2026-07-23), confirmed on real DuoCo hardware.** Branch `visualizer-review-fixes-2026-07-22`, off `main`, left undeleted per standing convention. This is the review doc's items #2, #3, and part of #6 from its own "how to make the end result more satisfying" ordering (item #1, the DP-grid early-bias root cause, is still open per item 24/25's "BeatGridPhase" lead; item #4, the unified latency-calibration flow, and item #5, P3 macro-dynamics, were not attempted this pass).
    - **A1/A2/A4 (`AudioDspProcessor.kt`, `RgbControllerViewModel.kt`):** new `AudioDspResult.flashFiredThisFrame: Boolean` field, set `true` only inside `triggerFlash()`'s two call sites (predictive mechanism 1, fast-causal mechanism 2) when a flash actually renders — not on `isBeat` (detection), which since item 24's P1 work fires up to ~180ms+`flashTimingOffsetMs` *after* the frame that actually carries the flash peak. Three consumers that were still keyed on `isBeat` and are now keyed on `flashFiredThisFrame` instead: `bleGattTransport.writeCommand`'s `bypassPacing` param (A1 — P2's wire-priority bypass was landing on an already-decayed frame, defeating its own purpose for exactly the flashes P1 made primary), `alternatingFlashBeatCounter++` in `broadcastAudioResultDirect` (A2 — the two-device call-and-response handoff was switching targets mid-decay, after the peak had already rendered on the wrong device), and the P0 `"AudioSync"` diagnostic log in `publishAudioDspResult` (was correlating against the wrong `cmdHex`). A4's white-flash desaturation write (`whiteHotFlashOffset`) moved out of the `isBeat` block entirely and into both `triggerFlash()` call sites, using `carriedFlashStrength * carriedFlashConfidence` (mechanism 1) or the same values additionally damped by `fastWeight` (mechanism 2, which has no detection-confirmed values of its own yet) — so the white "pop" and the brightness pop now render on the same frame instead of ~200ms apart.
    - **A3 (`AudioDspProcessor.kt`):** both trigger mechanisms used to scale their *entire* peak (`flashFloor + flashRange·…`) by `predictiveWeight`/`fastWeight`, silently defeating item 16's "every detected beat gets a minimum visible floor" guarantee at any confidence below 1.0 — including the first several seconds of every song and any loosely-timed real music (`bpmConfidence ≈ 0.5` is common). Changed to `flashFloor + (flashRange · …) · weight` in both mechanisms — the floor is now whole whenever a flash fires at all; only the variable portion scales with trust.
    - **A5 (`AudioDspProcessor.kt`):** `sustainIntensity` was recomputed fresh every frame from `(nowMs - sustainStateChangedAtMs)`, so a state flip mid-ramp (e.g. deactivating at 40% ramped-in) reset the timer *and* flipped which branch of the enter/exit formula computed intensity, producing an instantaneous jump (0.4 → 1.0) before decaying — a visible brightness step in the wrong direction, worst on Ambient Chill (`BRIGHTNESS_SWELL`). `sustainIntensity` is now a stored field that moves toward its 0/1 target by `dtMs / sustainRampMs` per frame, so a flip mid-ramp reverses smoothly from wherever it actually was.
    - **A6 (`VisualizerCaptureSource.kt`):** the `alreadyEnabledElsewhere` branch (item 22's fix for the shared-session-0 `setCaptureSize()` crash) inherits whatever `captureSize` the other client configured without checking it. If that inherited size is small enough that `numBins < 349`, every frame was silently dropped by the callback's own size guard while the attach watchdog's `receivedRealSignal` check (which only inspects raw byte content, not post-guard frame delivery) kept reporting success — a permanently dark visualizer with no error and no fallback. Now checked immediately after reading `alreadyEnabledElsewhere`: if the inherited `captureSize` is too small, release (without touching `enabled` — it's a shared handle) and retry through the same `attachRetryCount`/`MAX_ATTACH_RETRIES` path the real watchdog already uses, falling back to `onError`/simulation if exhausted.
    - **A7 (`RgbControllerViewModel.kt`):** `DemoAudioDspSimulator` constructs `AudioDspResult` with the defaulted P4 fields (`sat = 0f`, `value = 0f` — a known gap per item 23). `broadcastAudioResultDirect` now checks `hasHsvComponents = !(result.sat == 0f && result.value == 0f)` and falls back to Mirror (plain r/g/b) behavior for every device when it's false, instead of computing `hsvToRgb(_, 0f, 0f)` = pure black for every non-Mirror-role device the moment real capture fails and the demo engine takes over — previously the app would look like a broken lamp instead of a degraded-but-working fallback for any HueOffset/AlternatingFlash/BandSplit device.
    - **B1 (`AudioDspProcessor.kt`):** the predictive-flash fire condition (`nowMs >= scheduledFlashAtMs && predictiveWeight > 0.15f`) had no upper lateness bound — if confidence crossed 0.15 well after the target time, the flash fired immediately at that later moment instead of being skipped. Added `flashIsStale = (nowMs - scheduledFlashAtMs) > 80L` to the condition; a stale schedule is now simply skipped (mechanism 2 may still cover the beat) rather than firing arbitrarily late.
    - **Not attempted this pass, from the same review doc:** A8 (the ~210-220ms DP-grid early-bias root cause — still just the `"BeatGridPhase"` lead from item 24), B2 (frame-order-under-delay race), B3 (unifying `bluetoothDelayMs`/`flashTimingOffsetMs` into one measured latency), B4 (sample-count vs. time-based DSP windows — long-deferred, unchanged), B5 (per-frame allocation churn — flagged only, no profiling done), B6 (AudioRecord shutdown latency — documented, unchanged), and all of section C items 4 (guided latency-calibration flow), 5 (P3 macro-dynamics/drop detector), 7 (P5 saturation), 8 (B4 time-basing).
    - **Verification:** `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` both green, full suite unchanged at **260 tests** — these are timing/behavior corrections to existing, already-covered code paths (flash triggering, sustain ramping, role-transform fallback, shared-session attach), not new pure logic, consistent with how prior timing-only fixes in this file (items 17, 19, 22 §pre-dip-gate) were handled. **On-device verification for this pass is now covered by item 27 below** (same branch, follow-up session) — see there for what was actually confirmed on real hardware.

27. **`visualizer-review-2026-07-22.md` remaining items: A8 (root-caused + fixed, confirmed on real hardware), C4/B3 (latency knobs unified), C5/P3 (macro-dynamics), C7/P5 (saturation), C8/B4 (partial — auto-gain/UI-amplitude decay time-based; the four sample-count ring buffers deliberately left alone), B2 (frame-order race fixed) — COMPLETE, merged to `main` via item 28 below (2026-07-23), confirmed on real DuoCo hardware.** Same branch, `visualizer-review-fixes-2026-07-22` (2026-07-22/23). Device access became available this session (a physical Pixel 9 Pro XL, via `adb`) — used to actually capture real `BeatGridPhase` data for A8 rather than continuing to guess, and to smoke-test the full session's changes. **No physical DuoCo LED hardware was available in this session** — real BLE GATT writes, actual light output, and the two-device `AlternatingFlash` handoff (A1/A2, item 26) remain unconfirmed on real bulbs; what *was* confirmed is that the full audio-capture-through-DSP pipeline (real phone mic input, both trigger mechanisms, the drop detector, crest-based saturation, time-based auto-gain) runs for extended real-world capture sessions with no crashes, no exceptions, and the diagnostic logs showing sane values.
    - **A8 root-caused and fixed, not just investigated.** Built the branch, installed on-device, and ran the existing `AdbControlReceiver` harness (`select_backend phone_mic` + `start_metronome` at 120bpm) with diagnostics recording enabled, capturing real `BeatGridPhase` log lines. **The original hypothesis (DP scoring itself places grid nodes systematically ~200ms early) was not confirmed** — right after each `updateTempoAndGrid()` rebuild, `signedDeviationMs` was small (noise-level, roughly ±100-200ms), showing the DP's own beat placement tracks real onsets reasonably well. **What the data actually showed:** `BeatDetector.kt`'s `phaseAgreement` calculation did `gridBeats.minByOrNull { abs(it - evalTimestamp) }` — a literal search of whatever's *in* the list. Since `gridBeats` is only rebuilt once every `tempoUpdateIntervalMs` (1500ms), later real beats within the same window kept comparing against an increasingly stale last node, producing a sawtooth: `signedDeviationMs` near zero right after a rebuild, growing steadily more negative (observed down to **-1416ms** in one capture) until the next rebuild reset it. The previously-recorded "~200-220ms early" figure was the time-average of this sawtooth, not evidence of a fixed placement bug. **Fix:** replaced the raw list lookup with an extrapolated nearest-period-aligned lookup — `lastGrid + round((evalTimestamp - lastGrid) / periodMs) * periodMs` — the same idea `nextPredictedBeatMs` already uses to project past a stale `gridBeats.last()`, just allowed to move in either direction since this is "nearest" not "next." This only affects `phaseAgreement`/`confidence` (and the diagnostic log) — `nextPredictedBeatMs` itself was never actually vulnerable to the staleness bug, since its own forward-extrapolation loop already self-corrects regardless of how stale the list is; that's why the P1 predictive-flash timing itself was never found to be measurably wrong from this data. **Confirmed on real hardware, before/after the same 120bpm metronome test:** mean `signedDeviationMs` across a full capture went from **-589ms** (pre-fix) to **-53ms** (post-fix); mean absolute deviation from **607ms** to **136ms** (roughly the same order as `phaseAgreement`'s own `periodMs/4` tolerance window, i.e. now within the range the formula was designed for).
    - **C4/B3 (`AudioSettingsReducer.kt`, `CalibrationFlowReducer.kt`, `RgbControllerViewModel.kt`, `RgbUiState.kt`, `SettingsTabContent.kt`):** derived, not measured with new UI — re-reading the pipeline's own math showed the two knobs were never independent in the first place. `AudioDspProcessor` schedules a predictive flash's calculation at `nextPredictedBeatMs - flashTimingOffsetMs`; `queueAudioResult` then delays that frame's broadcast by `totalVisualDelayMs` (== `bluetoothDelayMs`) before it reaches the wire. The photon therefore appears at `nextPredictedBeatMs - flashTimingOffsetMs + bluetoothDelayMs`, which only lands exactly on the predicted beat when `flashTimingOffsetMs == bluetoothDelayMs` — previously two separately by-ear-tuned sliders (defaults 0 and 100) with no mechanism keeping them equal. `flashTimingOffsetMs` is now set to `bluetoothDelayMs`'s value at every site that sets the latter (`SetBluetoothDelayMs`, `SaveCalibrationAndExit`, `ResetCalibrationSettings`/`ResetAudioPipelineSettings`, the ViewModel's prefs-load in `init`, `onBluetoothAudioRouteActive`/`Inactive`) — the existing tap-to-sync "Calibrate" metronome flow is now the single guided measurement that sets both. `AudioSettingsState.flashTimingOffsetMs`'s default changed 100 → 0 to match. The standalone `SetFlashTimingOffsetMs` intent/reducer case and `setFlashTimingOffsetMs()` ViewModel method are left in place (unused by UI now) as a programmatic override, not deleted. `SettingsTabContent.kt`'s "Flash Timing Offset" slider was replaced with a read-only value display plus explanatory text pointing at the "Calibrate" button above it.
    - **C5/P3 macro-dynamics (`AudioDspProcessor.kt`):** implemented as a uniform (non-per-preset) mechanism, same rationale as stage 4's pre-dip, and deliberately *not* a literal percentile tracker over a rolling sample buffer (which would add real per-frame sort/allocation cost — exactly what B5 flags as a risk). Instead: `macroLoudnessCeiling`/`macroLoudnessFloor` are cheap EMA trackers (45s tau, in the doc's suggested 30-60s range) derived from the already-computed 6s `rollingMean` — ceiling attacks instantly and releases over ~45s, floor is the mirror image. `macroPercentile = (rollingMean - floor) / (ceiling - floor)` gives a 0..1 "where does now sit in the last 45s" reading, applied as a `0.85x-1.15x` brightness multiplier (`macroBoost`) to the ambient/ (Beat Only) flash-magnitude level only — not to the beat-flash envelope itself, which already has its own P1/P2/A3/B1 treatment. **Drop detector:** fires when `totalEnergy > macroLoudnessCeiling * 2.5f` (a judgment call, not measured) with a real ceiling already established and a 30s refractory. On fire: a guaranteed hue-anchor jump (bypasses the normal confidence gate — a drop doesn't need "was this really a beat"), a uniform white blast (`whiteHotFlashOffset = 0f`, not gated by `useWhiteFlash` — a drop moment should read as one regardless of preset, and reuses the existing per-preset `whiteFlashRecoveryMs` recovery rather than inventing a second decay), and a 2s `1.5x` boost to `flashRange` in both trigger mechanisms. New `"MacroDrop"` diagnostic log line.
    - **C7/P5 saturation (`AudioDspProcessor.kt`):** `sat` was previously driven only by `baseSat` (mid-band energy, backend-branched formula). Added a smoothed spectral-crest term (peak/mean magnitude ratio, computed in the same loop that already derives `frameAvgMag` — no extra spectrum pass) as a bounded `0-0.15` additive boost: a tonal/peaky passage reads more saturated than a noisy/broadband one at equal loudness. Uniform across presets. `crestRatio == 1.0` (flat spectrum) is a no-op, which is why the existing `baseSat coefficients diverge by backend` regression test (built on `uniformFrame`, inherently flat) was unaffected.
    - **C8/B4 (partial) (`AudioDspProcessor.kt`):** converted exactly the piece the review doc itself called "mechanical" — the auto-gain (`maxObservedBass`/`maxObservedMid`) and UI-amplitude (`maxUiObserved`/`maxPerBandObserved`) decay constants, previously fixed per-frame multipliers (0.97f/0.992f/0.98f) tuned against phone_mic's ~23ms/frame cadence, so they decayed roughly half as much real time per frame on Visualizer's ~50ms/frame cadence. Now `exp(-dtMs/tau)` with `tau` back-solved so the decay reduces to the exact original per-frame factor at phone_mic's reference interval — phone_mic behavior is bit-for-bit unchanged, Visualizer's is fixed. **Deliberately NOT done:** the four sample-count ring buffers (`energyHistory`/`shortHistory`/`noiseHistory`/`bassHistory`) remain fixed-size arrays, still genuinely divergent in real-time duration between backends. Converting these needs a structurally different (time-windowed, not fixed-size) accumulator and risks changing already-tuned feel in a way that can't be validated without dedicated on-device A/B listening on both backends — the review doc itself lists this as the lowest-priority, "fold into any future DSP touch" item, not a blocking one.
    - **B2 (`RgbControllerViewModel.kt`):** `queueCommand`/`queueAudioResult` used to launch one independent `viewModelScope.launch(Dispatchers.Default) { delay(...); broadcast... }` per frame when `totalVisualDelayMs > 0` — at ~43 frames/sec that's dozens of concurrently in-flight coroutines with no guarantee they'd resume in launch order, so two adjacent frames could occasionally broadcast out of order (a 1-frame flicker). Replaced with a single ordered `Channel<DelayedBroadcast>` (unlimited capacity) drained by one consumer coroutine that processes strictly FIFO, waiting out each item's remaining delay before the next — both `queueCommand` and `queueAudioResult` now share this, so ordering is structural rather than incidental. Consumer is started lazily on first use.
    - **Verification:** `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest` green throughout, full suite unchanged at **260 tests** (existing `AudioSettingsReducerTest.kt`/`CalibrationFlowReducerTest.kt` assertions updated for the new derived `flashTimingOffsetMs` values and extra persisted-pref effects; no new tests added elsewhere — same "needs real elapsed-wall-clock/hardware" rationale as every other timing-only change in this file). **On-device, this session (Pixel 9 Pro XL via `adb`):** two full `phone_mic` + synthetic-metronome capture sessions (pre-fix and post-fix) confirmed the A8 fix numerically; a third ~20s live capture with the fully-built branch (all of items 26+27's changes active) showed zero crashes, zero exceptions, and the process alive and logging normally throughout. **Not confirmed on real DuoCo BLE hardware in this session** (no physical bulbs available at the time) — the standing on-device gate (both backends, a percussive and an ambient preset, a two-device `AlternatingFlash` check) was carried forward as a blocker on merging to `main`. **Since resolved — see item 28.**

28. **`visualizer-review-fixes-2026-07-22` merged to `main` (merge commit `4473e53`, 2026-07-23), plus two more fixes (commit `6990fa7`) found and merged alongside it — real-DuoCo-hardware smoke test (BLE writes, two-device `AlternatingFlash` handoff) confirmed passing.** This closes out the standing on-device gate that items 26/27 both carried forward — the branch is no longer pending hardware confirmation.
    - **Two additional fixes, found uncommitted on the branch at merge time and folded in after confirming they were intended for this pass** (not part of the original items 26/27 write-up, dated 2026-07-23 in their own code comments):
      - **on_device permanent-stall-after-pause (`VisualizerCaptureSource.kt`):** item 25's bounded attach/exception retry budget (`MAX_ATTACH_RETRIES` = 5, ~7.5s total) was designed for *first-activation* stalls, but the exact same give-up-permanently behavior also fired on any *later* stall once real signal had already been seen once this session — e.g. pausing external media for longer than ~7.5s (a phone call, switching tracks) permanently fell back to demo simulation for the rest of the session, reading as "the visualizer never comes back." Fixed with a new `everReceivedRealSignal` sticky flag: once real signal has been confirmed at least once, the attach/exception watchdogs never exhaust their retry budget — they keep retrying indefinitely on a gentler `STALL_RECHECK_INTERVAL_MS` (4000ms) cadence instead of the fast first-activation `WATCHDOG_TIMEOUT_MS` (1500ms) cadence, until real signal returns.
      - **Non-audio write starvation (`DeviceWriteManager.kt`, `BleGattTransport.kt`):** P2's priority-aware supersede check (item 23) used a strict `>` comparison with a `0f` default `priority`, on the assumption every non-audio write only ever competes against other non-audio writes. That's false the moment a non-audio write (e.g. `restoreDeviceState()` when stopping music sync) needs to supersede an already-queued audio command of the same command-type byte enqueued with `priority = result.value` (0f..1f, per item 23's P4 role-transform work) — any nonzero audio priority would silently win over the default-0f restore write, leaving a device stuck showing the last live beat-flash color instead of the state that should have been restored. Fixed by changing `updateCommand`/`writeCommand`'s default `priority` from `0f` to `Float.MAX_VALUE`, so non-audio writes always supersede; only `RgbControllerViewModel.broadcastAudioResultDirect` (the one call site that wants a bounded, comparable priority) passes an explicit value.
    - **Verification:** full suite green at merge time and again post-merge on `main`, unchanged at **260 tests** (no new tests for either fix — same "needs real elapsed-wall-clock/hardware" rationale as every other timing-only change in this file; the priority-default fix is a one-line default-value change with no new branch to cover). Pushed to `origin/main`. Branch `visualizer-review-fixes-2026-07-22` left undeleted, per standing convention.

### RgbUiState shape (locked)

```
RgbUiState:
  connectivity: ConnectivityState
  coreControl: CoreControlState       // includes errorMessage, protocolBytes
  audioSettings: AudioSettingsState   // 40 fields (27 original + 13 from the mapping-layer stages 1-2, items 19-20), includes musicMode
  ambianceSettings: AmbianceSettingsState  // ambiancePreset non-null, "Balanced" default
  calibrationFlow: CalibrationFlowState
```

- `RgbIntent` lives inside `RgbUiState.kt` — there is **no separate `RgbIntent.kt` file**.
- `colorCalibrations`, `cctCalibrations`, `byteOverrides` stay as **separate StateFlows**, NOT part of `RgbUiState`.
- `TelemetryState` (`musicAmplitudes`, `visualizerHue`, `logMessages`, `deviceAchievedFps`) is separate from `RgbUiState` due to high-frequency updates.

### Current reducer status

All four reducers below are **wired live into `RgbControllerViewModel`** (originally on branch `wire-phase3b-reducers`, based on `phase3b-complete`; as of PR #13 (2026-07-20) this is merged into `refactor-checkpoint`, and as of PR #14 (2026-07-20) `main` is caught up too — see "Git/GitHub notes"). `RgbControllerViewModel._uiState` is a real `MutableStateFlow<RgbUiState>` (migrated in Stage 2/3, commits `24b60f3`/`8d39aac`), not the old flat `ControllerUiState`.

**Wiring shape:** a single `private fun dispatch(intent: RgbIntent)` in `RgbControllerViewModel.kt` runs every intent through all four reducers in sequence — `coreControlsReducer` → `ambianceSettingsReducer` → `audioSettingsReducer` → `calibrationFlowReducer` — threading the state through each call. This is safe because each reducer's `when` has an `else -> state to emptyList()` fallthrough, so an intent outside a reducer's scope is a no-op there; only the one reducer that actually owns the intent produces a state change or effects. Each reducer's side-effect list is executed immediately after by one dedicated executor function (`executeCoreSideEffect`, `executeAmbianceSideEffect`, `executeAudioSideEffect`, `executeCalibrationSideEffect`), which is where the real hardware/IO work (GATT calls, prefs, Room, coroutine loops) actually happens — the reducers themselves stay pure. Every public ViewModel method that used to mutate `_uiState` directly (e.g. `setPower`, `startScanning`, `toggleActiveControl`) is now a one-line `dispatch(RgbIntent.X(...))` call; hardware/IO logic that isn't a pure state transition was split into private `*Hardware()` helpers invoked from the executors (e.g. `connectDeviceHardware`, `startBleScanHardware`, `checkActiveAudioRouteHardware`).

`handleConnectionStateChange` (the real GATT callback) is **not** fully routed through the reducer — only its two state-mutation points now call `dispatch(RgbIntent.InternalConnectionStateChanged(...))`. The exponential-backoff retry logic, GATT connection-priority/MTU requests, and `discoverServices()` calls stay as direct ViewModel code, since the reducer's `InternalConnectionStateChanged` case doesn't (and shouldn't) model those — they're genuine hardware operations, not state transitions.

**The `StartMetronome` contract is now enforced, not just documented.** The metronome coroutine (`executeCalibrationSideEffect`'s `StartMetronome` case) calls `dispatch(RgbIntent.SendCalibrationFlash)` on every tick — the exact same call the public `sendCalibrationFlash()` makes — so `SendFlashPulse` is structurally producible from exactly one code path.

`CoreControlsReducer.kt` (`app/src/main/java/com/example/presentation/CoreControlsReducer.kt`) handles the Connectivity & Scanning / Device management / Core Controls intent groups. It has been **fixed, tested, merged, and wired**. Test coverage: 43 JUnit tests in `CoreControlsReducerTest.kt`, covering all 21 intents plus regression tests for both fixes described below.

**How this works:**

1. `SetPower`/`SetColor`/`SetBrightness`/`SetMode`/`SetWarmth` update `deviceStatesMap` via `applyControlledDeviceUpdates()`, which mirrors `RgbControllerViewModel.updateControlledDevicesInMap()`: it creates missing entries for every target address rather than only touching keys that already exist, so newly-connected auto-saved devices correctly get a `deviceStatesMap` entry from these five intents.
2. `SetDemoMode(true)` and `DisconnectAll` key off `activeConnections` (a live-only registry), mirroring the source ViewModel, rather than `deviceConnectionStates` (a map that never shrinks) — so the "already disconnected" fast path continues to fire correctly after the first device connects, avoiding repeated disconnect-effect emission for stale/already-disconnected addresses.

If touching this code further, reference the real `updateControlledDevicesInMap()` and `activeConnections` implementations in `RgbControllerViewModel.kt` directly — don't infer intended behavior from the reducer alone.

A pre-existing bug in `setWarmth` (writes `"CCT"` to a pref key, then immediately overwrites it with `"Colour"`) has been **intentionally preserved** in the reducer as behavior parity — this is not a bug to fix during the refactor.

`AmbianceSettingsReducer.kt` (`app/src/main/java/com/example/presentation/AmbianceSettingsReducer.kt`) handles the Ambiance & Camera Sync intent group. **Merged and wired.** Test coverage: 13 JUnit tests in `AmbianceSettingsReducerTest.kt`, covering all 8 intents. Source-diffed against the real `setAmbiance*()` / `applyAmbiancePreset()` handlers in `RgbControllerViewModel.kt` (lines 2775-2845) — no discrepancies found, unlike `CoreControlsReducer`.

**How this works:** 7 single-value setters (`SetAmbianceResponseSpeed`, `SetAmbianceSmoothnessMs`, `SetAmbianceSaturationBoost`, `SetAmbianceBrightnessCompensation`, `SetAmbianceUpdateRateCapFps`, `SetAmbianceSceneCutSensitivity`, `SetAmbianceNoiseDeadband`) each update one field and force `ambiancePreset` to `"Custom"`. `ApplyAmbiancePreset` is the exception: it sets `ambiancePreset` to the applied preset ID (not `"Custom"`), does **not** touch `ambianceUpdateRateCapFps`, and additionally cancels any running scene chain — modeled as `AmbianceSideEffect.CancelSceneChain`, mirroring `CoreSideEffect.CancelSceneChain`.

Deliberately **out of scope** for this reducer — verified, not overlooked:
- Ambiance capture start/stop has no ViewModel intent; `MainActivity.kt` drives `AmbianceCaptureService` directly via an `ActivityResultLauncher` (needs the Activity for the `MediaProjection` permission result).
- `WriteAmbianceColor` is a pure BLE passthrough (no `RgbUiState` field touched, fires per captured frame) — belongs with Commands/Protocol, not settings.
- `CoreSideEffect.StopAmbiance` already exists and is already wired into `CoreControlsReducer`, fired from `SetPower`/`SetColor`/etc. — not new work.

`AudioSettingsReducer.kt` (`app/src/main/java/com/example/presentation/AudioSettingsReducer.kt`) handles the Audio Settings intent group. **Merged and wired.** Ported against the real `setVisualizerPreset()`, `startMusicSync()`/`stopMusicSync()`, and the sensitivity/threshold/gain setters in `RgbControllerViewModel.kt` (lines 2677-2962, 3901-3975, 4009-4013). Source-diffed line-by-line against all 23 intent handlers, including the 8-preset × 16-field value table (byte-identical) — no discrepancies found *at the time*, but see the wiring-stage fix below. Test coverage: 39 JUnit tests in `AudioSettingsReducerTest.kt`, covering all 23 intents.

**How this works:**

1. `SetVisualizerPreset` writes the 16-field-per-preset value table (`visualizerConfigFor()`) verbatim, plus `activeFeatureName` — recomputed from `getVisualizerPresetName()`/current mode, mirroring source, even though the recompute is a no-op unless `musicMode` is `phone_mic`/`on_device`.
2. `StartMusicSync`/`StopMusicSync` model the real hardware/scene-runner work (`stopMusicSyncInternal`, `startAudioEngine`, per-address `saveDeviceState`/scene-runner release, `deviceAutomationMode` restore-or-clear) as `AudioSideEffect` cases for the ViewModel to execute — same pattern as `CoreSideEffect`. `startMusicSync` branches on `mode`: phone-mic/on-device modes route to `AudioSideEffect.StartAudioEngine`; the eight onboard BLE modes (`energetic_1/2`, `rhythm_1/2`, `spectrum_1/2`, `rolling_1/2`) route to three direct `BroadcastCommand` effects (toggle, style, sensitivity) instead.
3. Preserved faithfully, not "fixed": `setBeatThresholdMultiplier` and the smoothing/gamma/flash/brightness/speed/idle-delay group reset `visualizerPreset` to `"Custom"`; `setBeatCooldownMs`, the gain setters, `setAutoGainEnabled`, `setPaletteCyclingEnabled`, and `setLogarithmicScalingEnabled` do **not** — this inconsistency exists in the source and is reproduced exactly. `setBluetoothDelayMs` derives `totalVisualDelayMs = bluetoothDelayMs + BeatDetector().lookaheadMs` inline; `BeatDetector` (now `com.example.core.audio.BeatDetector` as of Phase 5, part B — see below) is always constructed with no args, so the reducer replicates the default as a local constant (`BEAT_DETECTOR_DEFAULT_LOOKAHEAD_MS = 180L`) rather than importing the class, to avoid this reducer depending on audio-pipeline internals for a single derived value.
4. **Bug found and fixed during wiring (not present in the original merge):** `ResetAudioPipelineSettings` was missing the `mid_flux_weight` prefs write that the real `resetAudioPipelineSettings()` (line 2938) performs — the in-memory field was reset but never persisted, so it would silently revert on the next app restart. The existing test had actually documented the *missing* write as an intentional "preserved quirk," which turned out to be wrong against real source. Both the reducer and `AudioSettingsReducerTest.kt` (`resetAudioPipelineSettings_persistsMidFluxWeight`) were corrected. Lesson: a prior source-diff pass missed this — don't assume "no discrepancies found" from an earlier session is still true without re-checking against the real ViewModel when you touch this code again.

`CalibrationFlowReducer.kt` (`app/src/main/java/com/example/presentation/CalibrationFlowReducer.kt`) handles the Calibration & Tuning intent group. **Merged and wired**, including the `StartMetronome` contract (see above — no longer doc-only). Ported against the real `startCalibrationMode()`/`stopCalibrationMode()`/`saveCalibrationAndExit()`/`sendCalibrationFlash()`/`resetCalibrationSettings()`/`toggleTestPattern()`/`setDevicePacing()`/`saveCctCorrectionProfile()`/`deleteCctCorrectionProfile()`/`saveColorCalibration()`/`deleteColorCalibration()` handlers in `RgbControllerViewModel.kt` (lines 1104-1149, 1775-1783, 1880-1965, 2964-2985, 3645-3657). Source-diffed line-by-line, branch-by-branch against all 13 intent handlers — one ordering mismatch found and fixed during verification (see below). Test coverage: 22 JUnit tests in `CalibrationFlowReducerTest.kt`, covering all 13 intents.

**How this works:**

1. `CalibrationFlowState` only has 3 fields (`showCalibrationPrompt`, `isCalibrationModeActive`, `calibrationDelayOffsetMs`), but the Calibration & Tuning intent group is broader than that shape — several intents write to `ConnectivityState` (`isTestPatternRunning`, `devicePacingMs`) and `AudioSettingsState` (`bluetoothDelayMs`, `totalVisualDelayMs`) instead of or in addition to `CalibrationFlowState`. `calibrationFlowReducer` touches `connectivity`, `audioSettings`, and `calibrationFlow` directly in the same function — same rationale as `AudioSettingsReducer` reaching into `deviceStatesMap`: the reducer is scoped by intent group, not by which state slice each intent happens to touch.
2. Almost every intent in this group is IO/hardware-bound (prefs, Room DB, live `DeviceWriteManager` mutation, or a long-running coroutine loop), so it leans on `CalibrationSideEffect` more heavily than the other reducers. `SaveColorCalibration`/`DeleteColorCalibration` are Room-only (no `RgbUiState` field touched, same pattern as `WriteAmbianceColor`); `SaveCctCorrectionProfile`/`DeleteCctCorrectionProfile` write prefs and reload the separate `cctCalibrations` StateFlow; `StartCalibrationMode`/`StopCalibrationMode`/`ToggleTestPattern` start or cancel long-running coroutines (metronome loop, test-pattern color cycle) that a pure reducer can't own directly.
3. Two extra parameters beyond `state`/`intent`, because a pure reducer can't do the IO or see live hardware state itself: `savedCalibrationDelayMs: Int` (pre-fetched pref read, seeds `calibrationDelayOffsetMs` when `StartCalibrationMode` fires) and `connectedManagerAddresses: Set<String>` (reproduces the `deviceWriteManagers[address] != null` guard in the source `setDevicePacing()` — when absent, the *entire* block including the state update is skipped, not just the hardware write).
4. Preserved faithfully, not "fixed": `StartCalibrationMode` unconditionally calls `stopCalibrationMode()` first, so restarting while already active silently resets and re-logs "stopped" then "started"; `SaveCalibrationAndExit` writes the same delay to two different pref keys via two different mechanisms; `ResetCalibrationSettings` writes `bluetooth_delay_ms` to 0 as an explicit separate pref call (none of its three `clear*Prefs()` calls cover that key) and resets pacing via two independent paths (live `DeviceWriteManager` mutation for connected devices, `devicePacingMs.mapValues { 100 }` in state for all recorded addresses) that are kept in sync manually, not derived from one another — both preserved, not unified.
5. The one mismatch caught during source-diff verification: `StartCalibrationMode`'s effect order initially had the metronome-start effect before the "Calibration Mode started..." log; source calls `addLog()` before launching the coroutine. Fixed to log-then-start, with a regression test (`startCalibrationMode_emitsStopThenStartEffectsInSourceOrder`) locking the order in.

## Hardware constraint (permanent, confirmed)

Every pixel-count change on a DuoCo device causes a visible firmware-level LED flash. This is unavoidable at the firmware level — design animations around it (e.g. deliberate stepped changes) rather than trying to eliminate it.

## Known open bug

LEDs freezing intermittently — unresolved. Experimental start/stop diagnostic logging (exportable to file) exists to help diagnose; if you touch BLE write paths or the watchdog, be aware this issue is still open.

## Ambiance mode

Uses screen capture (`MediaProjection`), **not** the physical camera. The camera exists in the codebase only as an unused calibration feature — don't assume camera code is load-bearing for ambiance.

## Deferred / known-not-urgent

- DSP/animation timing constants (`fluxHistorySize`, `noiseHistory`, `bassHistory`, `energyWindowSize`, `shortWindowSize`, per-frame offsets) are sample-count-based rather than time-based, causing behavior differences between `AudioRecord` (~43Hz) and the Visualizer API (~20Hz). Audited, fix scoped, not urgent. As of Phase 4, these constants live in `core/audio/AudioDspProcessor.kt` (branched by `AudioBackend`) rather than inline in the ViewModel, but the mismatch itself is unchanged.
- Firework splash experimental animation (two-device) — tuning ongoing, lives in Settings > Experimental.
- Smart Scene: chip dropdowns for in-place editing, animation timing consistency, chip label formatting, manual per-step editing — all deferred until scene editors are reconciled.
- Audio visualizer: `paletteCycling` on Beat Only pending hardware test decision; fire-effect flicker/brightness modulation deferred.

## Testing

- `CoreControlsReducerTest.kt` covers the Core Controls + Connectivity + Device management reducer (`CoreControlsReducer.kt`). 43 tests, all 21 intents.
- `AmbianceSettingsReducerTest.kt` covers the Ambiance & Camera Sync reducer (`AmbianceSettingsReducer.kt`). 13 tests, all 8 intents.
- `AudioSettingsReducerTest.kt` covers the Audio Settings reducer (`AudioSettingsReducer.kt`). 39 tests, all 23 intents, including regression tests for the selective `"Custom"`-preset-reset quirk and the `totalVisualDelayMs` derivation.
- `CalibrationFlowReducerTest.kt` covers the Calibration & Tuning reducer (`CalibrationFlowReducer.kt`). 23 tests, all 13 intents, including regression tests for the unconditional-restart quirk, the two-pref-key write in `SaveCalibrationAndExit`, the standalone `bluetooth_delay_ms` reset write, the dual pacing-reset paths, the no-live-manager no-op in `SetDevicePacing`, and the SendFlashPulse single-producer contract (see the `StartMetronome` contract in `CalibrationFlowReducer.kt`).
- Run via `./gradlew :app:testDebugUnitTest`. Reducer logic and its tests are opened as **separate PRs** (reducer first, tests based on the reducer branch) — follow this precedent for future reducer slices.
- Full suite was 140 tests as of the reducer-wiring work. These tests exercise the reducers in isolation only — they do **not** cover `RgbControllerViewModel`'s `dispatch()`/`execute*SideEffect()` wiring (effect ordering, hardware/coroutine interaction, demo-vs-real-hardware branching). That wiring was verified by build + source-diff against the pre-wiring ViewModel methods, not by new automated tests. A manual on-device smoke test of each control group was still outstanding at the time — this has since been run and confirmed (see "Git/GitHub notes"), but be aware the wiring itself still has no automated regression coverage, especially around BLE connect/disconnect and the calibration metronome.
- `AudioCaptureSourceTest.kt` (`hardware/audio/`) and `AudioDspProcessorTest.kt` (`core/audio/`) cover the Phase 4 audio isolation — 10 tests total (construction/contract for the capture sources + fake, deterministic DSP-math assertions via synthetic frames). Full suite was **150 tests** as of Phase 4. Same caveat as above: real on-device mic/Visualizer hardware behavior is unverified by these tests (Robolectric doesn't model real audio input) — the on-device smoke test of `phone_mic` and `on_device` music sync modes gating this has since been run and confirmed.
- **Post-Phase-4 cleanup (PR #15, 2026-07-20) added:**
  - `ConnectionManagerTest.kt` (`domain/`) — previously untested; covers connect/setConnected/disconnect state transitions, manual-disconnect tracking, per-address isolation.
  - `CalibrationMatrixSolverTest.kt` (`core/calibration/`) — covers both silent-fallback guards (`n < 3`, near-singular determinant) plus identity/scaling-fit correctness.
  - `DuoCoProtocolTest.kt` (`core/protocol/`) — closes the gap on the 14 previously-untested command builders (the highest-risk untested code in the app, since it encodes real BLE hardware commands) plus the mutable override get/set/clear state. `CoreExtractionTest.kt` still separately covers `createPowerCommand`/`createColorCommand`/`parseHex`/`formatHex` — don't duplicate those cases if you touch this again.
  - `FftTest.kt` (`hardware/audio/`) — closed-form impulse/DC/single-frequency transform assertions against the FFT moved verbatim in Phase 4.
  - `AudioDspProcessorTest.kt` expanded: Beat Only preset's brightness-floor behavior, auto-gain enabled/disabled divergence, and a beat trigger driven through the real `BeatDetector` autocorrelation pipeline (not just synthetic/mocked). Note if extending: `ColorConverter.hsvToRgb`'s cubic perceptual-brightness correction crushes low `value` readings toward 0 in the final RGB output, and a uniform-magnitude test frame always self-gates against its own noise floor once history saturates with that value — see the in-file comments before writing amplitude-based assertions.
  - Full suite is now **205 tests**.
- **Phase 5, part A (PR #17, 2026-07-20)** added `PacingAutoTuneEngineTest.kt` (`core/pacing/`) — 23 tests covering both evaluators and every state-machine branch of the extracted auto-tune algorithm. Full suite is now **228 tests**.
- **Phase 5, part B (PRs #19/#20, 2026-07-20):** step 1 added 3 tests to `AmbianceSettingsReducerTest.kt` covering `WriteAmbianceColor`/`SetAmbianceCaptureActive` (state-unchanged + correct side effect for each, including both `true`/`false` toggle directions). Step 2 added `DemoAudioDspSimulatorTest.kt` (`core/audio/`) — 4 tests covering rgb/hue/amplitude range invariants across repeated ticks, per-instance state isolation (two freshly-constructed simulators both produce valid output on their first tick), and the immediate-not-yet-idle behavior on a silenced first tick. No new tests for the verbatim `BeatDetector`/`MetronomePlayer` moves themselves (no logic changed, only location/package). Full suite is now **235 tests**.
- **Phase 6 (`phase6-godfile-extraction`, 2026-07-21):** `BleTransportTest.kt` (`hardware/ble/`) — 7 construction/contract tests against `FakeBleScanTransport`/`FakeBleGattTransport` (same style as `AudioCaptureSourceTest.kt`: wiring/contract only, not real hardware behavior). `CalibrationMatrixOpsTest.kt` (`core/calibration/`) — 11 tests covering `getCalibrationMatrices`/`interpolateMatrices` (previously untested): single-brightness fallback, malformed-JSON fallback, multi-brightness JSON parsing, non-numeric-key skipping, exact-match/linear/clamp-above/clamp-below interpolation, empty-map/single-entry fallbacks, and the disabled-stub passthrough. No new tests for the `SceneManager` or `AmbianceCommandSink` slices (pure relocations, no logic changed). Full suite is now **253 tests**.
- **Mapping-layer stage 1 (`mapping-stage1-time-basing-flash-params`, 2026-07-21):** one new test, `AudioDspProcessorTest.kt`'s `flashFloor and flashRange settings scale the beat flash peak`, plus updates to two existing `AudioSettingsReducerTest.kt` assertions (preset-table effect count, reset defaults) for the two new `AudioSettingsState` fields. Full suite is now **254 tests**.
- **Mapping-layer stage 2 (same branch, 2026-07-21):** two new `AudioDspProcessorTest.kt` cases isolating the anchor+breath mechanism (`a qualifying beat advances the hue anchor by hueAnchorJumpDeg`, `a beat below hueJumpConfidenceGate flashes but never moves the hue anchor`), plus `AudioSettingsReducerTest.kt`'s Punchy preset-table test extended for the 11 new fields (effect count 20 → 31). No hysteresis/ramp-timing test added — same rationale as stage 1's white-flash-recovery timing going untested. Full suite is now **256 tests**.
- **Mapping-layer stage 4 (same branch, 2026-07-21):** new `BeatDetectorTest.kt` — the class's first-ever test file (previously zero coverage), one test (`nextPredictedBeatMs is null before any tempo lock`) covering only the new field's no-lock default. No test for a real tempo-locked `nextPredictedBeatMs` value or the `AudioDspProcessor` pre-dip's actual output effect — driving a synthetic tempo lock realistically needs several seconds of periodic beats through the full autocorrelation/DP-grid pipeline, a materially bigger investment than anything else in this mapping-layer work. Full suite is now **257 tests**.
- **Post-mapping-layer follow-ups (same branch, 2026-07-21, item 22):** one new `AudioCaptureSourceTest.kt` case covering the `VisualizerCaptureSource` silent-attach watchdog's timeout constant (construction/contract-level only — the watchdog's actual reconstruct-on-silence behavior needs real Visualizer hardware, untestable under Robolectric). No test changes for the Smooth Flow brightness values (no prior test pinned them) or the pre-dip gate fix (pre-existing coverage of `audioFlashStrength`-gated behavior already exercised the corrected condition). Full suite is now **258 tests**.
- **Predictive flash scheduling + beat-detection tuning (`predictive-flash-scheduling` branch, 2026-07-22, item 24):** no new automated tests for the P1 scheduling logic, the `minBpm`/stale-lock/noise-floor fixes, or the fast-trigger noise gate — all timing/threshold-tuning changes on the same "needs real elapsed-wall-clock/hardware" basis as prior entries in this list; verified by build + the on-device adb-harness captures described in item 25 instead. The `BeatGridPhase`/`SignalLevel` diagnostic log additions are untested by design, same rationale as every prior `DiagnosticLogger.log()` addition. Full suite unaffected at 258 → 260 tests (net +2 from the on_device fix's own coverage, see below).
- **on_device signal-zero fix (same branch, item 25):** one new `AudioCaptureSourceTest.kt`-adjacent case plus the existing watchdog-timeout test were unaffected by the `retried`→`attachRetryCount` rename (private implementation detail, no test referenced it). The fix itself was verified live on real hardware via the adb test harness (see item 25), not by a new unit test — Robolectric cannot model real Visualizer signal content. Full suite is **260 tests**.
- **`visualizer-review-2026-07-22.md` fixes (`visualizer-review-fixes-2026-07-22` branch, item 26):** no new automated tests. All six fixes (A1/A2/A4's `flashFiredThisFrame` re-keying, A3's floor-scaling correction, A5's sustain-ramp continuity, A6's shared-session captureSize guard, A7's demo-fallback role fix, B1's staleness cutoff) are timing/behavior corrections to code paths already covered by existing tests (`AudioDspProcessorTest.kt`, `AudioCaptureSourceTest.kt`), and none of the existing assertions pin the specific values these changes touch (no test asserted `isBeat`-vs-`flashFiredThisFrame` timing, exact `sustainIntensity` values, or `broadcastAudioResultDirect`'s role-fallback branch). Full suite unchanged at **260 tests**. Same "needs real elapsed-wall-clock/hardware" rationale as every prior timing-only change in this list for why nothing new was added — verification here was build + full suite green only, with the on-device smoke test explicitly outstanding (see item 26's own note).
- **`visualizer-review-2026-07-22.md` remaining items (same branch, item 27):** no new automated tests either — A8's fix (BeatDetector's `phaseAgreement` nearest-beat lookup), C5's macro-dynamics, C7's crest-based saturation, C8's partial time-basing, and B2's ordered-channel fix are all timing/mechanism corrections in the same category as item 26's. Existing `AudioSettingsReducerTest.kt`/`CalibrationFlowReducerTest.kt` assertions *were* updated (not added to) for the C4/B3 derived-value change, since those tests directly pinned the old independent `flashTimingOffsetMs` defaults/effects that no longer hold. Full suite unchanged at **260 tests**. Unlike item 26, this one **does have real on-device verification** — see item 27's own "Verification" paragraph for the before/after A8 numbers and the crash-free live capture — but still no automated regression coverage for any of it, and still no real DuoCo BLE hardware exercised.

## Local tooling notes

- **`adb` is installed but not on PATH in this Claude Code environment (Windows/Git Bash).** Binary lives at `/c/Users/attgm/AppData/Local/Android/Sdk/platform-tools/adb.exe` (standard Android Studio SDK location under `%LOCALAPPDATA%`) — `where adb`/`which adb` both fail, so it has to be invoked by full path.
  - **`adb devices`** works fine by full path with no special handling.
  - **Pulling files (`adb pull <remote> <local>`) needs `MSYS_NO_PATHCONV=1`** set first — otherwise Git Bash rewrites the leading `/` in the *remote* device path (e.g. `/sdcard/Android/data/...`) into a bogus Windows path (`C:/Program Files/Git/sdcard/...`) before adb ever sees it, and the pull fails with "No such file or directory" for a path that's real on the device.
  - **Installing an APK (`adb install`) needs the OPPOSITE — do NOT set `MSYS_NO_PATHCONV`** (or explicitly `unset` it first) — `adb.exe` is a native Windows binary and needs the *local* APK path (e.g. `app/build/outputs/apk/debug/app-debug.apk`) converted to a real Windows path (`C:\Users\...`), which is exactly the conversion `MSYS_NO_PATHCONV` disables. With it set, `install` fails the same "No such file or directory" way, just for the opposite reason (local path unconverted instead of remote path over-converted).
  - Net effect: toggle `MSYS_NO_PATHCONV` per-command depending on whether the path that matters is on the device (`pull`, `shell ls`, etc. → set it) or on this machine (`install`, `push`'s local source → unset/don't set it). `push` needs both handled correctly since it has one path of each kind.
  - The app's saved diagnostics live at `/sdcard/Android/data/<applicationId>/files/diagnostics/diagnostics_log_<epochMs>.txt` — `applicationId` is `com.aistudio.expressivergb.xkzqp` (`app/build.gradle.kts`), **not** `com.example` (the Kotlin package name) — `adb shell run-as com.example` or browsing under that name will fail with "unknown package." List/pull directly from the `Android/data/<applicationId>/files/diagnostics/` path instead of trying `run-as`.
  - Log line timestamps in diagnostics exports are `SystemClock.elapsedRealtime()` (ms since boot, monotonic — see `DiagnosticLogger.kt`'s `logInternal`), **not** wall-clock. Several tags (e.g. `FlashSchedule`'s `atMs`/`targetMs`) separately embed `System.currentTimeMillis()` wall-clock values in their own message text. These two clocks are NOT the same number space — cross-referencing a bracket timestamp against an embedded `atMs`-style value requires first computing the constant per-session offset (`bracket_ts - embedded_wallclock_ts` from any line that has both) and applying it, not comparing the raw numbers directly. Confirmed stable to ~2ms across a 30-60s capture (no deep-sleep drift during active foreground use), so one offset sample per session is enough.

## Git/GitHub notes

- **`main` and `refactor-checkpoint` are both up to date and equivalent** as of PR #21 (merge commit `03d9eaa`, 2026-07-20). Sequence: PR #19 (Phase 5, part B, step 1 — close the `getActiveInstance()` escape hatch) merged into `refactor-checkpoint` (merge commit `22ba8be`); PR #20 (Phase 5, part B, step 2 — BeatDetector/MetronomePlayer/demo-DSP extraction) merged into `refactor-checkpoint` (merge commit `f24acc5`); PR #21 then promoted `refactor-checkpoint` → `main` (clean merge, content diff was exactly PRs #19+#20's changes, no other divergence). `phase5-close-getactiveinstance-escape-hatch` and `phase5-beatdetector-metronome-demodsp-extraction` are fully merged and were left undeleted, per standing convention. Prior to this, PR #18 (merge commit `8b0e8e1`) had brought both branches equivalent through Phase 5, part A: PR #15 (post-Phase-4 cleanup) merged into `refactor-checkpoint` (merge commit `769c092`); PR #16 carried that into `main`; PR #17 (Phase 5, part A — tab + pacing extraction) merged into `refactor-checkpoint` (merge commit `c6290eb`) after a source-diff confirmed the bundled "connecting…" overlay fix matched its PR description; PR #18 promoted that to `main`. `cleanup-post-refactor-audit` and `phase5-tab-and-pacing-extraction` (plus its two component branches `phase5-mainactivity-tabs`/`phase5-pacing-autotune-extraction`) are also fully merged and undeleted. **Re-verify with `git merge-base --is-ancestor` (or `git log origin/main..origin/refactor-checkpoint`) before assuming this is still true** — branch pointers here go stale as soon as new work lands on either branch, and this file will not update itself.
- `refactor-checkpoint` was deleted (local + `origin`) on 2026-07-22 — see the note near the bottom of this section. Historical references to it below are accurate for the phase they describe; there is no longer a branch to check out or sync.
- Phase 4 branch `phase4-hardware-isolation` was based on `wire-phase3b-reducers`, **not** `refactor-checkpoint` — at the start of Phase 4, `refactor-checkpoint` still predated RgbUiState/dispatch() entirely, so it was the wrong base despite an initial handoff note suggesting it. Its PR (#12) targeted `wire-phase3b-reducers`, which was then merged into `refactor-checkpoint` via PR #13 once Phase 4 landed.
- The Gradle wrapper (`gradlew`, `gradlew.bat`, `gradle/wrapper/`, `gradle/gradle-daemon-jvm.properties`) is committed to the repo.
- If using AI Studio's own GitHub panel elsewhere in this workflow: it silently overwrites custom `LICENSE`/`README` with its own template on repo (re-)init — always check/restore both files after any AI-Studio-driven git re-init. This does not apply to Claude Code's own git operations, just noting it in case AI Studio touches the same repo.
- `.gitignore` covers `debug.keystore` and `debug.keystore.base64` — keep both covered if keystore handling changes.
- **As of 2026-07-20, Claude Code (this environment) owns routine git/GitHub mechanics going forward** — merging approved PRs, keeping this file current — without checking in for each individual step. Merge commits (never squash) remain the convention; branches are kept, not deleted, after merge **except where explicitly approved otherwise** (see the `refactor-checkpoint` deletion below — an intentional exception, confirmed with Joe first, not a reversion of this rule). Still flag before acting on anything destructive, anything where a merge isn't a clean fast-forward/no-conflict case, or anything that bundles an actual app-behavior change (like the connecting-overlay fix above) rather than pure restructuring.
- **Phase 6 is promoted — `main` and `refactor-checkpoint` are both up to date and equivalent again** as of PR #22 (merge commit `689f3e9`, 2026-07-21), fast-forwarded into `refactor-checkpoint` immediately after. Build sequence on `phase6-godfile-extraction` (based on `main`) before promotion: `phase6-calibration-matrix-extraction` and `phase6-ambiance-sink-di` were built directly off it; `phase6-ble-hardware-extraction` was also built off it, and `phase6-scene-orchestration` was built off `phase6-ble-hardware-extraction` (not the shared base) since scene orchestration needed BLE's finished transport interface first. All four were merged into `phase6-godfile-extraction` with merge commits (order: scene-orchestration-with-BLE first, then calibration, then ambiance-sink-di last since it had the only real conflicts — the DI constructor/`init`/companion-object region that the BLE slice had also touched). Conflicts were resolved by hand, verified by re-running the full test suite (253 passing) and `assembleDebug` after resolution. PR #22 then promoted `phase6-godfile-extraction` → `main` following developer confirmation that the on-device smoke test (BLE scan/connect/disconnect/reconnect, ambiance capture, scene apply, calibration flow) had been run. All Phase 6 sub-branches (`phase6-ble-hardware-extraction`, `phase6-calibration-matrix-extraction`, `phase6-scene-orchestration`, `phase6-ambiance-sink-di`, `phase6-godfile-extraction`) are fully merged and left undeleted, per standing convention.
- **Color-conversion dedup merged to `main` via PR #23 (merge commit `1148672`, 2026-07-21).** Build + full 253-test suite green; source-diffed against `main` to confirm the moved gamma functions are byte-identical to their old private copies (no behavior change) and that the `hsvToRgb` divergence documented above is real and was deliberately left alone rather than papered over. **The on-device smoke test gate was explicitly waived for this merge by Joe's choice** — every prior phase touching this pipeline gated on it; this one didn't. Branch left undeleted, per standing convention.
- **Gradle wrapper bump to 9.5.0 committed directly to `main` (commit `189d819`, 2026-07-21)**, as its own commit separate from the color-conversion merge. Re-applied via AGP Upgrade Assistant after an earlier accidental checkout wiped the original bump. `gradle.properties`/`gradle/libs.versions.toml` checked and confirmed unrelated to this change (not bundled in). `./gradlew assembleDebug` run and confirmed green after commit, before push.
- **BLE GATT lifecycle "extraction" request (2026-07-21) resolved as no-op after verification.** Investigated per a request to move `handleConnectionStateChange`/`handleServicesDiscovered`/etc. into `hardware/ble/`; source-diff found Phase 6 already did this (see item 12 above) — no branch opened, nothing to merge.
- **`predictive-flash-scheduling` merged to `main` (merge commit `18795e8`, 2026-07-22, no PR opened — merged locally and pushed directly per the standing "Claude Code owns routine git/GitHub mechanics" convention above).** Covers item 24 (P1 predictive scheduling, beat-detection tuning) and item 25 (the on_device signal-zero fix + adb test harness), plus the design docs referenced throughout this file's history that had never actually been committed (`audit-findings-2026-07-20.md`, `mapping-proposal-audio-to-led-2026-07-21.md`, `visualizer-review-2026-07-21.md`). Full 260-test suite green on `main` post-merge; clean merge, no conflicts. Branch left undeleted and pushed to `origin`, per standing convention. `refactor-checkpoint` was **not** synced as part of this — it was already behind `main` before this session (a pre-existing drift, not something this merge introduced or was asked to fix).
- **`refactor-checkpoint` deleted (local + `origin`), 2026-07-22, with Joe's explicit go-ahead.** Verified before deleting, both locally and against `origin`, via `git merge-base --is-ancestor refactor-checkpoint main` (and the same check against the `origin/*` copies): `refactor-checkpoint` was a **strict ancestor of `main`** on both — every commit reachable from it was already reachable from `main`, and nothing on it was unique. Deleting the branch pointer doesn't touch any commit; all of that history is still in `main`'s log exactly as it was. `main` is now the sole active line of work — no more secondary integration branch. If a future session ever needs to check whether some other stale branch is safe to delete the same way, that `git merge-base --is-ancestor <branch> main` check is the pattern to reuse.
- **`visualizer-review-fixes-2026-07-22` merged to `main` (merge commit `4473e53`, 2026-07-23, no PR opened — merged locally and pushed directly per the standing "Claude Code owns routine git/GitHub mechanics" convention).** Covers items 26-28: the `visualizer-review-2026-07-22.md` fixes (A1-A8, B1/B2, C4/C5/C7/C8), plus two additional fixes (commit `6990fa7`, the on_device permanent-stall-after-pause fix and the `DeviceWriteManager`/`BleGattTransport` non-audio-write-starvation fix) that were found uncommitted on the branch at merge time and folded in after confirming with Joe they were intended for this pass — see item 28 for detail on both. Full 260-test suite green pre-merge (on the feature branch) and again post-merge on `main`; clean merge via the `ort` strategy, no conflicts. Gated on the real-DuoCo-hardware smoke test (BLE writes, two-device `AlternatingFlash` handoff) — confirmed passing before this merge. Branch left undeleted and pushed to `origin`, per standing convention.
